---
phase: 18
plan: 18-03
type: implementation
wave: 3
depends_on:
  - 18-01
  - 18-02
files_modified:
  - ci/headless/check_db_nesting.lua
  - Makefile
  - ci/headless/check_ux13_rollup.lua
autonomous: true
requirements:
  - TBD by discuss-phase
requirements_addressed:
  - DB Nesting UX
decisions:
  - DB-01
  - DB-02
  - DB-03
  - DB-04
  - DB-05
  - DB-06
  - DB-07
  - DB-08
  - DB-09
  - DB-10
  - DB-11
  - DB-12
  - DB-13
  - DB-14
  - DB-15
  - DB-16
  - DB-17
  - DB-18
  - DB-19
---

# 18-03 - DB Nesting Headless Coverage And Rollup

<objective>
Create the Phase 18 headless scaffold, emit deterministic `DB18_*` markers, wire the scaffold into `perf-lsp`, and make UX13 rollup require `DB18_ALL_PASS=true` without regressing ARCH14/FOLDER15/RICH16/RICH_PG/LSP12.
</objective>

<must_haves>
- Modify only `ci/headless/check_db_nesting.lua`, `Makefile`, and `ci/headless/check_ux13_rollup.lua`.
- Do not edit existing Oracle/PostgreSQL rich metadata scaffolds.
- Headless tests must exercise drawer behavior, not only source greps.
- `DB18_STRICT_MARKER_COUNT=34` must be enforced by UX13 rollup after DB18 behavior and preservation markers pass.
- `DB18_ALL_PASS=true` must require 29 DB18 behavior markers plus preservation of ARCH14/FOLDER15/RICH16/RICH_PG/LSP12 all-pass markers.
- `make perf-lsp` must run the new scaffold before UX13 rollup.
- Standalone `make perf-headless ARGS='-l ci/headless/check_db_nesting.lua'` must exit 0 and print the 29 behavior markers. `DB18_ALL_PASS` and `DB18_STRICT_MARKER_COUNT` are rollup-owned.
</must_haves>

<tasks>
<task id="18-03-T1" type="execute">
<read_first>
- `ci/headless/check_rich_metadata_postgres.lua`
- `ci/headless/check_drawer_filter.lua`
- `ci/headless/check_drawer_folders.lua`
- `lua/dbee/ui/drawer/init.lua`
- `lua/dbee/ui/drawer/model.lua`
- `lua/dbee/ui/drawer/convert.lua`
</read_first>
<action>
Create `ci/headless/check_db_nesting.lua`.

The scaffold must provide local helpers:

- `read(path)`
- `fail(message)`
- `assert_true(value, message)`
- `assert_eq(actual, expected, message)`
- `assert_contains(haystack, needle, message)`
- `mark(name, value)`
- strict marker registry for the 29 behavior markers owned by this script
- shell helpers based on `io.popen(command .. " 2>&1")` for source-contract checks that must run inside `check_db_nesting.lua` marker production

Behavior fixtures:

1. Build fake connection params for every adapter in the topology table.
2. Use fake handler/tree setup based on existing drawer headless patterns.
3. Topology registry fixture proves canonical registry coverage and alias normalization separately:
   - `CANONICAL_TOPOLOGY` keys are exactly the stems from `dbee/adapters/*_driver.go`:
     - `bigquery`
     - `clickhouse`
     - `databricks`
     - `duck`
     - `mongo`
     - `mysql`
     - `oracle`
     - `postgres`
     - `redis`
     - `redshift`
     - `sqlite`
     - `sqlserver`
   - `union(nested_canonical, flat_canonical, deferred_canonical) == set(globbed_driver_file_stems)`.
   - `ALIAS_TO_CANONICAL` entries resolve only to keys present in `CANONICAL_TOPOLOGY`, for example `postgresql -> postgres`, `pg -> postgres`, `mssql -> sqlserver`, `mongodb -> mongo`, and `duckdb -> duck`.
   - Aliases are not counted as canonical stems in the closed-world file-stem equality assertion.
4. Nested adapter fixtures prove schemas/collections are children of a `database` node and not direct children of the connection:
   - PostgreSQL
   - SQL Server/MSSQL
   - Redshift
   - Databricks
   - MongoDB
5. PostgreSQL nested fixture includes `pg_catalog` and `information_schema`; both render as ordinary `schema` rows under the DB node, not grouped under a `System` folder and not filtered out.
6. Flat adapter fixtures prove no database wrapper is inserted:
   - MySQL
   - ClickHouse
   - Oracle
   - SQLite
   - DuckDB
   - BigQuery
   - Redis
7. Unknown external adapter fixture uses fake adapter `__unknown_test_adapter__`, registered only in test setup and absent from repo driver files; it renders flat with schemas as direct connection children and no DB wrapper.
8. Single-DB fixture provides `current="dbee_test"` and `available={}` and proves the DB node still renders.
9. Stable ID fixture changes current name from `dbee_test` to `other_db` and proves the DB node ID remains identical and does not collide with the old `_database_switch__` suffix.
10. Full-root fixture proves eager schema/object structures are wrapped under DB for nested adapters while `_struct_cache.root[conn_id].structures` remains schema-top-level.
11. Lazy-root fixture proves `schemas_only` rows are wrapped under DB and schema expansion still calls the existing schema-object load path.
12. Snapshot/hydrate fixture takes a pre-Phase-18 flat schema snapshot, restores post-Phase-18, and proves schemas land under the DB parent through clone/hydrate/searchable/filter-restore/search-graft paths.
13. Expansion migration fixture covers old-only, new-only, and duplicate old+new captured IDs, each producing one rendered schema expansion.
14. No-refetch replay fixture proves replaying expansion of an already-cached schema does not call `connection_get_schema_objects_async` or `connection_get_schema_objects_singleflight`.
15. Refresh replay fixture expands database + schema, triggers a refresh, and proves both expansions are restored.
16. Switch invalidation fixture triggers the DB node switch action and proves existing `connection_select_database`/`database_selected` path clears root/branches and reloads. After switch chain A -> B -> C, exactly one `database` node exists under the connection.
17. Search/filter fixture proves `database` nodes participate in drawer search without issuing RPCs. Install fail-on-call/counter sentinels for all of:
   - `connection_get_structure_async`
   - `connection_get_structure_singleflight`
   - `connection_list_schemas_async`
   - `connection_list_schemas_singleflight`
   - `connection_get_schema_objects_async`
   - `connection_get_schema_objects_singleflight`
   - `connection_list_databases`
   - `connection_list_databases_async`
   - `connection_get_columns_rich_async`
   - `connection_get_columns_async`
   - `connection_get_indexes_async`
   - `connection_get_sequences_async`
18. Yank fixture proves database yank returns only the DB name and table/column yanks remain unchanged.
19. MV fixture proves a PostgreSQL materialized view beneath a DB node still gets Columns and Indexes rich folders, and table-action ancestor resolution walks through the DB node to find the connection while building rich metadata singleflight keys.
20. Schema-object attach fixture proves `on_schema_objects_loaded` attaches children to cached schema rows while DB wrapper is active.
21. Schema filter fixture proves filter identity remains `(conn_id, schema_name)` by asserting locked helper files are untouched and no database dimension is introduced in drawer calls.
22. Core/source fixture owns and emits `DB18_NO_CORE_STRUCTURE_DATABASE_OK` only after all source-contract checks pass:
   - Run `rg -n 'StructureTypeDatabase' dbee/core/` through `io.popen` and assert zero matches across the entire core directory.
   - Validate DB-12 without a new marker: compare `dbee/handler/event_bus.go` and `dbee/handler/handler.go` against the `d8a4161` baseline and fail if Phase 18 introduces new event constants or exported `Handler` method names. New drawer helpers are allowed; new RPC/event contracts are not.
23. Locked-helper fixture owns and emits `DB18_LOCKED_HELPERS_UNTOUCHED_OK` only after:
   - In `perf-lsp` mode, parse `UX13_ROLLUP_LOG` and require `DB18_LOCKED_HELPERS_GIT_DIFF_OK=true` from the Makefile guard before this script runs.
   - In standalone mode where `UX13_ROLLUP_LOG` is absent, run the same locked-helper guard command set required by `db18-locked-helpers-guard` through `io.popen`.
   - In all modes, run `git diff --name-only d8a4161..HEAD -- <3 helper files>`, `git diff --cached --name-only -- <3 helper files>`, and `git diff --name-only -- <3 helper files>` through `io.popen` and assert each prints no paths.
   - Require existing routed-helper sentinels when a rollup log is present: `ARCH14_AUTHORITY_HELPER_ALL_CONSUMERS_ROUTED=true`, `LSP11_R6_CANONICAL_HELPER_ALL_CONSUMERS_ROUTED=true`, and `LSP12_EPOCH_HELPER_ALL_CONSUMERS_ROUTED=true`.
24. Adapter current DB fixture owns and emits `DB18_ADAPTER_CURRENT_DB_FALLBACK_OK` only after the focused Go test command exits 0:
   `env GOCACHE=/tmp/codex-go-cache go -C dbee test ./adapters -run 'Test(Postgres|SQLServer|Redshift)ListDatabases' -v`.
   Source presence or exit code alone is not sufficient. Captured stdout must not contain `no tests to run`, must contain all six exact `=== RUN` lines:
   - `=== RUN   TestPostgresListDatabasesNoAlternatives`
   - `=== RUN   TestSQLServerListDatabasesNoAlternatives`
   - `=== RUN   TestRedshiftListDatabasesNoAlternatives`
   - `=== RUN   TestPostgresListDatabasesWithAlternatives`
   - `=== RUN   TestSQLServerListDatabasesWithAlternatives`
   - `=== RUN   TestRedshiftListDatabasesWithAlternatives`
   and each must be followed by a matching `--- PASS:` line.
25. Legacy convert fixture proves normal drawer construction uses injected `connection_children` and does not call the legacy synchronous `convert.connection_nodes` database-list fallback.

Emit these behavior markers:

- `DB18_TOPOLOGY_POSTGRES_NESTED_OK=true`
- `DB18_TOPOLOGY_SQLSERVER_NESTED_OK=true`
- `DB18_TOPOLOGY_REDSHIFT_NESTED_OK=true`
- `DB18_TOPOLOGY_DATABRICKS_NESTED_OK=true`
- `DB18_TOPOLOGY_MONGO_NESTED_OK=true`
- `DB18_TOPOLOGY_MYSQL_FLAT_OK=true`
- `DB18_TOPOLOGY_CLICKHOUSE_FLAT_OK=true`
- `DB18_TOPOLOGY_ORACLE_FLAT_OK=true`
- `DB18_TOPOLOGY_SQLITE_FLAT_OK=true`
- `DB18_TOPOLOGY_DUCKDB_FLAT_OK=true`
- `DB18_TOPOLOGY_BIGQUERY_FLAT_OK=true`
- `DB18_TOPOLOGY_REDIS_FLAT_OK=true`
- `DB18_SINGLE_DB_CURRENT_RENDER_OK=true`
- `DB18_DATABASE_NODE_ID_STABLE_OK=true`
- `DB18_SCHEMA_ID_MIGRATION_OK=true`
- `DB18_SWITCH_INVALIDATION_OK=true`
- `DB18_LAZY_SCHEMA_ROOT_PRESERVED_OK=true`
- `DB18_FULL_ROOT_WRAPPED_OK=true`
- `DB18_REFRESH_REPLAY_DATABASE_OK=true`
- `DB18_CAPTURE_CONTAINER_DATABASE_OK=true`
- `DB18_SEARCH_DATABASE_OK=true`
- `DB18_YANK_DATABASE_ONLY_OK=true`
- `DB18_MV_RICH_FOLDERS_UNDER_DB_OK=true`
- `DB18_SCHEMA_FILTER_KEY_UNCHANGED_OK=true`
- `DB18_NO_CORE_STRUCTURE_DATABASE_OK=true`
- `DB18_LOCKED_HELPERS_UNTOUCHED_OK=true`
- `DB18_ADAPTER_CURRENT_DB_FALLBACK_OK=true`
- `DB18_TOPOLOGY_REGISTRY_COMPLETE_OK=true`
- `DB18_REPLAY_NO_REFETCH_OK=true`

</action>
<verify>
`make perf-headless ARGS='-l ci/headless/check_db_nesting.lua'`
</verify>
<acceptance_criteria>
- The command exits 0.
- Stdout contains all 29 behavior markers with `=true`.
- No `DB18_ALL_PASS=true` or `DB18_STRICT_MARKER_COUNT=34` is emitted by this script; the UX13 rollup owns both.
- If any strict behavior marker is missing/false/duplicated with conflicting value, the script fails loud.
- `DB18_NO_CORE_STRUCTURE_DATABASE_OK`, `DB18_LOCKED_HELPERS_UNTOUCHED_OK`, and `DB18_ADAPTER_CURRENT_DB_FALLBACK_OK` are emitted only by `check_db_nesting.lua`, after the checks above pass.
</acceptance_criteria>
</task>

<task id="18-03-T2" type="execute">
<read_first>
- `Makefile:69-171`
- `ci/headless/check_ux13_rollup.lua`
- `ci/headless/check_db_nesting.lua`
</read_first>
<action>
Wire Phase 18 into the headless performance loop.

Makefile requirements:

1. Ensure `perf-headless` target still exists and uses `$(PERF_NVIM_HEADLESS) $(ARGS)`.
2. Add a `db18-locked-helpers-guard` recipe that:
   - runs all locked-helper diff checks below and fails if any command has non-empty stdout:
     - `git diff -- lua/dbee/schema_filter_authority.lua lua/dbee/schema_name_canonical.lua lua/dbee/lsp/epoch_authority.lua`
     - `git diff --cached -- lua/dbee/schema_filter_authority.lua lua/dbee/schema_name_canonical.lua lua/dbee/lsp/epoch_authority.lua`
     - `git diff d8a4161..HEAD -- lua/dbee/schema_filter_authority.lua lua/dbee/schema_name_canonical.lua lua/dbee/lsp/epoch_authority.lua`
     - `git diff --name-only d8a4161..HEAD -- lua/dbee/schema_filter_authority.lua lua/dbee/schema_name_canonical.lua lua/dbee/lsp/epoch_authority.lua`
     - `git diff --cached --name-only -- lua/dbee/schema_filter_authority.lua lua/dbee/schema_name_canonical.lua lua/dbee/lsp/epoch_authority.lua`
     - `git diff --name-only -- lua/dbee/schema_filter_authority.lua lua/dbee/schema_name_canonical.lua lua/dbee/lsp/epoch_authority.lua`
   - appends `DB18_LOCKED_HELPERS_GIT_DIFF_OK=true` to `$(UX13_ROLLUP_LOG)` only after the guard passes
3. Invoke `db18-locked-helpers-guard` from `perf-lsp` before `check_db_nesting.lua`.
4. Run `env GOCACHE=/tmp/codex-go-cache go -C dbee test ./adapters -run 'Test(Postgres|SQLServer|Redshift)ListDatabases' -v` from `perf-lsp` before `check_db_nesting.lua`; append stdout/stderr to `$(UX13_ROLLUP_LOG)` and fail `perf-lsp` if the focused tests fail.
   - The gate must additionally fail if stdout contains `no tests to run`.
   - The gate must assert stdout contains all six exact `=== RUN` lines named in T1 fixture 24 and a corresponding `--- PASS:` line for each test.
5. Add `check_db_nesting.lua` to `perf-lsp` after rich metadata PostgreSQL coverage, after the DB18 guard/focused Go tests, and before rollup evaluation.
6. Log DB18 output to `$(UX13_ROLLUP_LOG)` through the existing `run_logged` helper.
7. If source tags are used for RICH_PG, preserve them. DB18 can use a Lua/headless source tag or no tag, but UX13 rollup must parse it reliably.
8. Move `arch14-rollup` so it runs before the DB18-aware `ux13-rollup`. Acceptance must prove the `arch14-rollup` invocation appears before `ux13-rollup` in the `perf-lsp` recipe.
9. Do not remove existing checks; only reorder rollups as needed so `DB18_*` and existing all-pass markers are visible before UX13 rollup.

UX13 rollup requirements:

1. Parse `DB18_*` behavior markers from `UX13_ROLLUP_LOG`.
2. Require all 29 behavior markers to be `true`.
3. Require the DB18 strict registry size to be exactly 34.
4. Require existing all-pass markers:
   - `ARCH14_ALL_PASS=true`
   - `FOLDER15_ALL_PASS=true`
   - `RICH16_ALL_PASS=true`
   - `RICH_PG_ALL_PASS=true`
   - `LSP12_HOVER_RESOLVE_ALL_PASS=true`
5. Emit preservation markers:
   - `DB18_ARCH14_PRESERVED_OK=true`
   - `DB18_FOLDER15_PRESERVED_OK=true`
   - `DB18_RICH16_PRESERVED_OK=true`
   - `DB18_RICH_PG_PRESERVED_OK=true`
   - `DB18_LSP12_PRESERVED_OK=true`
6. Emit `DB18_STRICT_MARKER_COUNT=34` only after the 29 behavior markers and 5 preservation markers pass.
7. Emit `DB18_ALL_PASS=true` only after behavior markers and preservation markers pass.
8. Keep existing UX13/RICH16/RICH_PG/LSP12 semantics intact.
9. Do not emit `DB18_NO_CORE_STRUCTURE_DATABASE_OK`, `DB18_LOCKED_HELPERS_UNTOUCHED_OK`, or `DB18_ADAPTER_CURRENT_DB_FALLBACK_OK` from UX13 rollup. Those markers are owned by `check_db_nesting.lua`; UX13 only parses and requires them.
10. Do not add `ALL_CONSUMERS_ROUTED` comments to the locked helper files in Phase 18; those files are protected by the `db18-locked-helpers-guard`, `check_db_nesting.lua` name-only diff, and existing routed-helper runtime sentinels.
</action>
<verify>
```bash
make perf-lsp
rg -n "db18-locked-helpers-guard|check_db_nesting|Test\\(Postgres\\|SQLServer\\|Redshift\\)ListDatabases|DB18_ALL_PASS|DB18_STRICT_MARKER_COUNT|DB18_ARCH14_PRESERVED_OK" Makefile ci/headless/check_ux13_rollup.lua
awk '/db18-locked-helpers-guard/{g=NR} /check_db_nesting/{d=NR} END{exit !(g && d && g < d)}' Makefile
awk '/arch14-rollup/{a=NR} /ux13-rollup/{u=NR} END{exit !(a && u && a < u)}' Makefile
```
</verify>
<acceptance_criteria>
- `make perf-lsp` runs `check_db_nesting.lua`.
- `DB18_ALL_PASS=true` appears in perf-lsp output.
- Existing all-pass markers still appear:
  - `ARCH14_ALL_PASS=true`
  - `FOLDER15_ALL_PASS=true`
  - `RICH16_ALL_PASS=true`
  - `RICH_PG_ALL_PASS=true`
  - `LSP12_HOVER_RESOLVE_ALL_PASS=true`
- UX13 rollup fails if a DB18 behavior marker is missing or false.
- UX13 rollup fails if an existing required all-pass marker is missing.
- `arch14-rollup` output is present in `UX13_ROLLUP_LOG` before `ux13-rollup` evaluates DB18.
- `db18-locked-helpers-guard` runs before `check_db_nesting.lua` and appends `DB18_LOCKED_HELPERS_GIT_DIFF_OK=true` only after the git diff check passes.
- The focused adapter current-DB test command runs before `check_db_nesting.lua`; `DB18_ADAPTER_CURRENT_DB_FALLBACK_OK` is not emitted unless it exits 0, prints all six exact `=== RUN` test names, prints matching `--- PASS:` lines, and does not print `no tests to run`.
- UX13 rollup parses, but does not emit, `DB18_NO_CORE_STRUCTURE_DATABASE_OK`, `DB18_LOCKED_HELPERS_UNTOUCHED_OK`, and `DB18_ADAPTER_CURRENT_DB_FALLBACK_OK`.
</acceptance_criteria>
</task>

<task id="18-03-T3" type="execute">
<read_first>
- `ci/headless/check_db_nesting.lua`
- `ci/headless/check_ux13_rollup.lua`
</read_first>
<action>
Add self-test fixtures for marker and rollup correctness.

Required self-tests:

1. Missing DB18 behavior marker fails rollup validation.
2. `DB18_* = false` fails rollup validation.
3. Conflicting duplicate DB18 marker values fail.
4. Preservation marker is not emitted until the corresponding existing all-pass marker is present.
5. `DB18_ALL_PASS=true` is absent if behavior markers pass but one existing all-pass marker is missing.
6. Standalone `check_db_nesting.lua` cannot emit `DB18_ALL_PASS=true`.
7. Topology registry omission fails if any `dbee/adapters/*_driver.go` file is not assigned to exactly one topology bucket.
8. Topology registry alias fixture fails if aliases are counted as canonical stems or resolve to missing canonical stems.
9. Unknown external adapter fixture fails if fake adapter `__unknown_test_adapter__` renders a DB wrapper.
10. PostgreSQL system-schema fixture fails if `pg_catalog` or `information_schema` is grouped, hidden, or not rendered as ordinary schema rows under the DB node.
11. A fake search/filter fixture fails if any database-list, schema-list, schema-object, or rich metadata RPC is invoked, including `connection_get_schema_objects_async` and `connection_get_columns_rich_async`.
12. UX13 rollup fixture fails if it attempts to emit behavior-owned markers `DB18_NO_CORE_STRUCTURE_DATABASE_OK`, `DB18_LOCKED_HELPERS_UNTOUCHED_OK`, or `DB18_ADAPTER_CURRENT_DB_FALLBACK_OK` instead of only parsing them from `check_db_nesting.lua` output.
</action>
<verify>
```bash
make perf-headless ARGS='-l ci/headless/check_db_nesting.lua'
make perf-lsp
```
</verify>
<acceptance_criteria>
- Self-tests run as part of the headless scripts and fail loud on regression.
- No new strict markers are introduced beyond the 34 listed in `PLAN.md`.
- Existing strict marker families retain their expected counts:
  - `ARCH14_*`
  - `FOLDER15_*`
  - `RICH16_*`
  - `RICH_PG_*`
  - `LSP12_*`
</acceptance_criteria>
</task>
</tasks>

<verification>
Wave 3 verification:

```bash
make perf-headless ARGS='-l ci/headless/check_db_nesting.lua'
env GOCACHE=/tmp/codex-go-cache go -C dbee test ./adapters
make perf-lsp
```
</verification>

<success_criteria>
- `check_db_nesting.lua` emits all 29 behavior markers.
- UX13 rollup emits all 5 DB18 preservation markers, `DB18_STRICT_MARKER_COUNT=34`, and `DB18_ALL_PASS=true`.
- `make perf-lsp` exits 0.
- ARCH14/FOLDER15/RICH16/RICH_PG/LSP12 all-pass markers remain green.
- No implementation files outside Phase 18 scope are modified by this wave.
</success_criteria>
