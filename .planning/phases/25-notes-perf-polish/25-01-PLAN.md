<files>
MODIFY lua/dbee/notes_migration.lua
NO_TOUCH lua/dbee/schema_filter_authority.lua
NO_TOUCH lua/dbee/schema_name_canonical.lua
NO_TOUCH lua/dbee/lsp/epoch_authority.lua
NO_REPO_TEST_FILES benchmark harness is inline headless Lua or temporary /tmp only
</files>

<tasks>
T0 - Baseline audit and guard setup
- Confirm `promote_staging()` currently writes the promote manifest at `lua/dbee/notes_migration.lua:969` and rewrites it with `promote_complete=true` at `:993-994`.
- Confirm Phase 25 does not remove or weaken the second manifest write.
- Confirm final `ensure_dir(final_dir)` currently runs inside the per-entry promotion loop at `lua/dbee/notes_migration.lua:974-977`.
- Confirm staged size is recorded by `file_size(staged_path)` at `lua/dbee/notes_migration.lua:929` after `copy_file()` has already read the source buffer at `:263`.
- Confirm Phase 23 locked helper files have no diff before and after implementation.

T1 - Copy-size dedup
- Change `copy_file(src, dst)` to return copied byte length on success, for example `true, nil, #data`.
- Update `stage_global_notes()` to store `src_size_bytes` from the returned byte length instead of `file_size(staged_path)`.
- Preserve the `entries` manifest schema exactly: `src_staging`, `src_basename`, `planned_dst`, `collision_suffix`, and `src_size_bytes`.
- Keep `verify_staging()` size checks stat-backed so partial writes or path mismatches still fail before promotion.
- Document/retain the invariant that no supported external writer touches `staging_dir/**` between `stage_global_notes()` and `verify_staging()`; the migration lock plus randomized staging token owns that tree.

T2 - Keep Phase 23 manifest protocol
- Keep building the full manifest in memory inside `promote_staging()`.
- Keep the first manifest write with `promote_complete=false` before final-dir hoist and before any final rename.
- Keep `manifest.promote_complete = true` and the post-rename `write_json_atomic(promote_manifest_path(notes_dir), manifest)` after all renames succeed.
- Keep `complete_after_promotion()` responsible for sentinel write and manifest cleanup.
- Verify `ci/headless/check_folder_scoped_notes.lua` GN23 expectations remain compatible without editing GN23 tests.

T3 - Final directory ensure hoist
- After the first manifest write succeeds and before the promote rename loop, build a deterministic set/list of unique `dirname(entry.planned_dst)` values.
- Call `ensure_dir(final_dir)` once per unique final directory before any final rename.
- If any hoisted ensure fails, return `nil, "final_dir_mkdir_failed"` exactly as today and do not start final renames.
- Keep rollback behavior scoped to `promote_rollback_paths`; do not delete pre-existing final folders or notes.
- Keep staging root and staged namespace directory creation unchanged.

T4 - Zero-byte recovery fix
- Update promote-manifest validation so `file_size(entry.planned_dst) == nil` means missing, while `0` is accepted when `tonumber(entry.src_size_bytes) == 0`.
- Update recovery-manifest validation so `file_size(final_path) == nil` means missing, while `0` is accepted when `recovery.final_sizes[final_path] == 0`.
- Add a zero-byte global SQL note fixture that exercises normal migration, promote recovery after all renames before sentinel, and recovery-needed after sentinel write failure.
- Emit `ORA25_ZERO_BYTE_OK=true` only after all zero-byte cases pass.

T5 - Crash/recovery preservation checks
- Verify crash after first manifest write but before any rename leaves manifest present, sentinel absent, global notes intact, and retry/inspect behavior unchanged.
- Verify crash after partial promotion preserves manifest and does not write sentinel.
- Verify crash after all renames but before sentinel is recoverable through `handle_promote_manifest()` using final-path size validation, including zero-byte notes.
- Verify sentinel write failure still writes `.notes-migration-v1.recovery-needed` and next launch validates final paths before sentinel.
- Verify existing `promote_complete=true` and `promote_complete=false` manifest readers both remain compatible.

T6 - Headless benchmark harness and correctness assertions
- Use a synthetic notes directory under `vim.fn.tempname()` or `/tmp`, never the user's real state directory.
- Generate `notes/global/*.sql` for each cohort and include zero-byte file, spaces/unicode filename, a larger note file over 10KB, and an existing-destination/collision cohort.
- Generate a fake handler with `source_conn_lookup = {}`, `get_sources()`, and `list_all_folder_ids_across_sources()` returning exactly the generated folder IDs with count `1`.
- Generate a fake folder-capable source with `supports_folders()`, `load_folders()`, and `_ensure_folders_loaded()` that sets `_folders_load_state = "loaded_ok"` and exposes the same folder IDs/names.
- If a `connection_get_folders` shaped helper is used by the harness, it returns a `{ folder_id, folder_name }` list matching the generated folders.
- Run `require("dbee.notes_migration").maybe_run(handler, notes_dir, {})` against fresh temp dirs.
- Assert `maybe_run == true`, sentinel exists, legacy `notes/global/` is gone or backed up, and every generated folder namespace contains exactly `G` migrated `.sql` files before any budget marker can print.
- Emit `LIVE_PG25_MIGRATION_MS=<cohort>:<variant>:<n>` per iteration, discard warmups, then emit cohort-qualified P50/P95 diagnostics.

T7 - Paired baseline versus optimized measurement
- Run baseline and optimized variants on identical generated corpora with the same `G`, `F`, filenames, file sizes, warmup count, measured iteration count, and temp-root strategy.
- Revised cohorts:
  - small: `G=5`, `F=2`, `C=10`, P50 <= 80ms.
  - medium: `G=20`, `F=5`, `C=100`, P50 <= 200ms.
  - large: `G=50`, `F=10`, `C=500`, P50 <= 700ms.
- Treat the Phase 23 diagnostic shape `G=100`, `F=10`, `C=1000` target of about 280ms as diagnostic unless measured proof supports making it strict.
- Require a structured strict summary or equivalent checks proving every required cohort emitted `n`, warmup count, P50, P95, budget, and pass/fail.
- Print `ORA25_MIGRATION_BUDGET_OK=true` only when all revised strict cohort budgets pass and no cohort metric is missing/malformed.

T8 - Phase 23 preservation gate
- Run the existing Phase 23 rollup if available: `make gn23-rollup`.
- If the rollup target is unavailable in the executor environment, run the direct GN23 grep/marker commands from `.planning/phases/23-folder-scoped-notes/23-03-PLAN.md` and report the gap.
- Confirm `GN23_STRICT_MARKER_COUNT=94` and `GN23_ALL_PASS=true` remain intact.

T9 - Scope and locked-helper gate
- Run `git diff -- lua/dbee/schema_filter_authority.lua lua/dbee/schema_name_canonical.lua lua/dbee/lsp/epoch_authority.lua` and require no output.
- Run `git diff --name-only` and require implementation changes to list only `lua/dbee/notes_migration.lua` after excluding Phase 25 planning artifacts.
- Confirm `notes_migration_inspect` and `notes_migration_cleanup_backups` references remain unchanged outside `lua/dbee/notes_migration.lua`.
</tasks>

<acceptance>
- `lua/dbee/notes_migration.lua` is the only implementation file modified.
- Manifest write count on the successful non-empty promotion path remains two.
- The first manifest write remains before final-dir hoist and before final rename; the second write persists `promote_complete=true` after all renames.
- Final destination `ensure_dir` count is `F` unique final folders, not `C` promoted entries.
- Hoist failure returns `final_dir_mkdir_failed` and performs no final rename.
- Staged entry `src_size_bytes` is derived from copied buffer length with no manifest schema change.
- Verification and recovery still stat staged/final paths and sizes.
- Zero-byte SQL notes migrate and recover without false missing-file rejection.
- Per-connection local-note migration behavior is unchanged.
- Bench success cannot print from the zero-folder path or missing-cohort metrics.
- Phase 23 GN23 rollup still emits 94 strict markers and `GN23_ALL_PASS=true`.
- `ORA25_ZERO_BYTE_OK=true`, `ORA25_MIGRATION_BUDGET_OK=true`, and `PHASE25_ALL_PASS=true` are emitted only after all gates pass.
</acceptance>

<strict_markers>
COUNT_TARGET 3
STRICT ORA25_ZERO_BYTE_OK=true
STRICT ORA25_MIGRATION_BUDGET_OK=true
STRICT PHASE25_ALL_PASS=true
DIAGNOSTIC LIVE_PG25_MIGRATION_MS=<cohort>:<variant>:<n>
DIAGNOSTIC ORA25_MIGRATION_MS_P50=<cohort>:<variant>:<n>
DIAGNOSTIC ORA25_MIGRATION_MS_P95=<cohort>:<variant>:<n>
</strict_markers>

<must_haves>
- Do not implement O1. Do not remove the post-promotion `promote_complete=true` manifest rewrite.
- Keep existing manifest field names and compatible values.
- Keep the migration sentinel as the only lifecycle-complete artifact.
- Keep all folder namespace construction and validation routed through existing Phase 23 helpers.
- Keep the three locked helpers untouched.
- Keep benchmark artifacts out of the repository unless a later approved scope change adds them.
</must_haves>
