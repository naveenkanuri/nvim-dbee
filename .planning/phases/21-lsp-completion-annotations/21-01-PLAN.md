---
phase: 21
plan: 21-01
type: implementation
wave: 1
depends_on: []
files_modified:
  - lua/dbee/lsp/schema_cache.lua
autonomous: true
requirements:
  - DBEE-FEAT-05
requirements_addressed:
  - Column completion PK/FK/null annotations
  - LSP 3.17 labelDetails plus legacy detail compatibility
decisions:
  - Annotation surface REC-DEFAULT
  - Composite FK rendering REC-DEFAULT
  - Null direction REC-DEFAULT
  - Default-expression marker deferred
---

# 21-01 - Column Completion Annotation Rendering

<objective>
Pre-bake rich metadata annotations into cached column completion items while keeping `label` and `insertText` equal to the raw column name.
</objective>

<must_haves>
- Keep `item.label = col.name` and `item.insertText = col.name`.
- Set `item.labelDetails.detail` for modern LSP 3.17 clients.
- Set `item.detail` for older clients that ignore `labelDetails`.
- Do not add default-expression popup markers in this wave.
- Do not change disk cache version or serialized column payloads.
- Do not edit locked helpers.
</must_haves>

<tasks>
<task id="21-01-T1" type="execute">
<read_first>
- `.planning/phases/21-lsp-completion-annotations/21-CONTEXT.md`
- `dbee/core/types.go:227-257`
- `lua/dbee/lsp/schema_cache.lua:1192-1213`
- `lua/dbee/lsp/schema_cache.lua:2230-2238`
- `lua/dbee/ui/drawer/convert.lua:137-191`
</read_first>
<action>
Add local annotation helpers in `schema_cache.lua` near the completion item helpers:

1. `rich_fk_target_columns(ref)`:
   - Prefer `ref.target_columns` when it is a non-empty list.
   - Fall back to `{ ref.target_column }`.
   - Never render both the shorthand and array values.
2. `rich_fk_marker(ref)`:
   - Skip malformed refs with no target table or no target column(s).
   - Single target: `[FK→target_table.target_col]`.
   - Composite target: `[FK→target_table.(a,b)]`.
   - Omit target schema from popup markers to keep the completion row compact.
3. `column_annotation_markers(col, pk_count)`:
   - PK:
     - if `col.primary_key == true` and `pk_count > 1` and `primary_key_ordinal > 0`, render `[PKn]`;
     - otherwise render `[PK]`.
   - FK:
     - iterate `col.foreign_keys` only if it is a list;
     - render all distinct valid FK markers, space-joined;
     - dedupe by constraint name + target table + target columns.
   - Nullable:
     - append `null` only when `col.nullable == true` and the column is not PK.
     - omit when nullable is false or nil.
   - Unsupported rich metadata adapters therefore produce no annotation text.
4. `column_completion_detail(col, markers)`:
   - return `col.type` when no markers exist;
   - otherwise return `col.type .. " " .. table.concat(markers, " ")`.
</action>
<acceptance_criteria>
- Marker planned: `LSP21_PK_SINGLE_MARKER_OK`.
- Marker planned: `LSP21_PK_COMPOSITE_ORDINAL_OK`.
- Marker planned: `LSP21_NULLABLE_TRUE_MARKER_OK`.
- Marker planned: `LSP21_PK_NULL_SUPPRESSED_OK`.
- Marker planned: `LSP21_NOT_NULL_OMITTED_OK`.
- Marker planned: `LSP21_FK_SINGLE_MARKER_OK`.
- Marker planned: `LSP21_FK_COMPOSITE_TARGET_TUPLE_OK`.
- Marker planned: `LSP21_FK_MULTIPLE_REFS_OK`.
- Marker planned: `LSP21_CAPABILITY_FALSE_EMPTY_RICH_FIELDS_OMIT_OK`.
</acceptance_criteria>
</task>

<task id="21-01-T2" type="execute">
<read_first>
- `lua/dbee/lsp/schema_cache.lua:1192-1213`
- `lua/dbee/lsp/schema_cache.lua:2750-2768`
- `lua/dbee/lsp/server.lua:65-128`
</read_first>
<action>
Update `_update_column_index(schema, table_name)` to use the helpers.

Required shape:

```lua
{
  label = col.name,
  kind = CompletionItemKind.Field,
  labelDetails = markers_text ~= "" and { detail = " " .. markers_text } or nil,
  detail = column_completion_detail(col, markers),
  insertText = col.name,
  sortText = "0_" .. col.name,
}
```

Implementation details:

1. Precompute `pk_count` from the table's columns before building items.
2. Keep `label` raw because `_copy_items_with_data` uses `item.label` to populate `data.column`.
3. Keep `sortText` stable.
4. Do not mutate returned items at read time; `get_column_completion_items` already returns deep copies.
5. Do not touch `server.lua` unless an implementation-time test proves the server strips `labelDetails`.
</action>
<acceptance_criteria>
- Marker planned: `LSP21_COMPLETION_LABEL_UNCHANGED_OK`.
- Marker planned: `LSP21_LABELDETAILS_DETAIL_RENDERED_OK`.
- Marker planned: `LSP21_DETAIL_COMPAT_RENDERED_OK`.
- `get_column_completion_items(..., { include_data = true })` still returns `data.kind == "column"` with the raw column identity.
</acceptance_criteria>
</task>
</tasks>

<verification>
Wave-local smoke after implementation:

```bash
make lsp21
make lsp21-locked-helpers-guard
```

If Wave 4 test targets do not exist yet, run the focused ad hoc command added with Wave 4 once available:

```bash
make perf-headless ARGS='-l ci/headless/check_lsp21_completion_annotations.lua'
```
</verification>

<success_criteria>
- Column labels remain raw names.
- Modern clients can render `labelDetails.detail`.
- Older clients can render rich `detail`.
- Unsupported rich metadata fields silently omit annotations.
- No disk cache version bump.
</success_criteria>

