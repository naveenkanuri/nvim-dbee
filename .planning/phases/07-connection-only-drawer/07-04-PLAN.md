---
phase: 07-connection-only-drawer
plan: 04
type: execute
wave: 4
depends_on: ["07-02", "07-03"]
files_modified:
  - ci/headless/check_connection_lifecycle.lua
  - ci/headless/check_connection_coordination.lua
  - .github/workflows/test.yml
autonomous: true
requirements: [DCFG-01]

must_haves:
  truths:
    - "Every new Phase 7 lifecycle and drawer behavior ships with headless coverage. No D-64 through D-88 behavior is accepted on grep-only evidence alone"
    - "Phase 7 tests must drive real `DrawerUI` paths, real handler event listeners, and real encoded Phase 6 structure identities where replay/rebuild behavior matters; do not stub past the very seams the plan is claiming to own"
    - "Phase 4 D-31 and Phase 6 D-30 through D-63 remain regression-guarded during Phase 7. The connection-only drawer rewrite must not silently break filter snapshot restore, drawer-owned root fencing, or bounded materialization"
    - "CI continues to run Phase 6 suites and adds dedicated Phase 7 lifecycle/coordination suites instead of replacing old checks"
  artifacts:
    - path: "ci/headless/check_connection_lifecycle.lua"
      provides: "headless coverage for lifecycle invalidation surface, connection-only root behavior, dispatcher ownership, and fail-closed actions"
      contains: "DCFG01_DRAWER_LIFECYCLE_ALL_PASS"
    - path: "ci/headless/check_connection_coordination.lua"
      provides: "headless coverage for single-flight, invalidation batching, sticky selection, reconnect continuity, and async database-switch placeholder patching"
      contains: "DCFG01_COORDINATION_ALL_PASS"
    - path: ".github/workflows/test.yml"
      provides: "CI execution of the new Phase 7 suites alongside existing Phase 6 regression suites"
      contains: "check_connection_lifecycle.lua"
  key_links:
    - from: "ci/headless/check_connection_lifecycle.lua"
      to: "lua/dbee/ui/drawer/init.lua"
      via: "the suite drives real DrawerUI actions, handler events, and D-31 filter restore behavior against the connection-only drawer"
      pattern: "DrawerUI"
    - from: "ci/headless/check_connection_coordination.lua"
      to: "lua/dbee/lsp/init.lua"
      via: "the suite proves single-flight, backpressure, rewarm, and reconnect coordination across handler, drawer, and LSP"
      pattern: "connection_get_structure_async"
---

<objective>
Add headless proof and CI coverage for the full Phase 7 contract.

Purpose: make Phase 7 execute-ready and reviewable with the same rigor as Phase 6. By the end of this plan, the new lifecycle surface, connection-only drawer behavior, coordination contracts, reconnect continuity, and async `database_switch` all have machine-readable proof, and the shipped Phase 6 suites still run as regression guards.
</objective>

<execution_context>
@/Users/naveenkanuri/Documents/nvim-dbee/.codex/skills/gsd-plan-phase/SKILL.md
@/Users/naveenkanuri/Documents/nvim-dbee/.codex/get-shit-done/workflows/plan-phase.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-drawer-navigation/04-CONTEXT.md
@.planning/phases/06-structure-laziness-notes-picker/06-CONTEXT.md
@.planning/phases/07-connection-only-drawer/07-CONTEXT.md
@.planning/phases/07-connection-only-drawer/07-RESEARCH.md
</context>

<constraints>
- Honor Phase 4 D-31, Phase 5 D-29, Phase 6 D-30 through D-63, and Phase 7 D-64 through D-88 verbatim.
- Use real DrawerUI refresh/expand/filter paths, real handler event listeners, and real `convert.structure_node_id(...)` identities in replay/rebuild-sensitive assertions.
- Keep existing Phase 6 suites (`check_structure_lazy.lua`, `check_notes_picker.lua`, and `check_drawer_filter.lua` where appropriate) as regression guards; do not replace them with weaker Phase 7-only coverage.
- Machine-readable markers are required for each new suite.
- CI must run the new Phase 7 suites and the pre-existing Phase 6 suites together.
</constraints>

<interfaces>
Lifecycle suite markers:
```text
DCFG01_INVALIDATION_PAYLOAD_OK=true
DCFG01_SILENT_RELOAD_OK=true
DCFG01_FAILURE_EVENT_OK=true
DCFG01_PARTIAL_FAILURE_OK=true
DCFG01_BOOTSTRAP_SNAPSHOT_OK=true
DCFG01_CONNECTION_ONLY_ROOT_OK=true
DCFG01_ACTION_TARGETING_OK=true
DCFG01_SOURCE_EDIT_REACHABLE_OK=true
DCFG01_TEST_FAIL_CLOSED_OK=true
DCFG01_REFRESH_MODE_OK=true
DCFG01_CURRENT_CONN_VISUAL_OK=true
DCFG01_PHASE6_FILTER_REGRESSION_OK=true
DCFG01_DRAWER_LIFECYCLE_ALL_PASS=true
```

Coordination suite markers:
```text
DCFG01_SINGLE_FLIGHT_OK=true
DCFG01_WAITER_FANOUT_OK=true
DCFG01_BOOTSTRAP_REPLAY_OK=true
LIFECYCLE01_BOOTSTRAP_POST_SNAPSHOT_OK=true
LIFECYCLE01_BOOTSTRAP_TAIL_OK=true
LIFECYCLE01_BOOTSTRAP_OVERFLOW_OK=true
LIFECYCLE01_BOOTSTRAP_OVERFLOW_STORM_OK=true
DCFG01_SUPERSEDED_FLIGHT_OK=true
DCFG01_WAITER_CLEANUP_OK=true
DCFG01_BACKPRESSURE_OK=true
DCFG01_STICKY_SELECTION_OK=true
DCFG01_RECONNECT_CONTINUITY_OK=true
DCFG01_DATABASE_SWITCH_ASYNC_OK=true
DCFG01_DATABASE_SWITCH_STALE_DROP_OK=true
DCFG01_PHASE6_STRUCTURE_REGRESSION_OK=true
DCFG01_COORDINATION_ALL_PASS=true
```
</interfaces>

<tasks>
<task type="auto">
  <id>07-04-01</id>
  <title>Add lifecycle and connection-only drawer headless coverage</title>
  <files>ci/headless/check_connection_lifecycle.lua</files>
  <read_first>
    - .planning/phases/07-connection-only-drawer/07-CONTEXT.md
    - .planning/phases/07-connection-only-drawer/07-VALIDATION.md
    - ci/headless/check_structure_lazy.lua
    - lua/dbee/ui/drawer/init.lua
    - lua/dbee/handler/init.lua
    - lua/dbee/config.lua
  </read_first>
  <action>
    Add a dedicated headless suite for the lifecycle foundation and drawer rewrite.

    1. Create `ci/headless/check_connection_lifecycle.lua` using the Phase 6 headless pattern.

    2. Drive real surfaces:
       - real `DrawerUI`
       - real handler event listeners
       - real connection-only root rendering
       - real dispatcher mode entry points
       - real filter close/restore path for D-31 regression coverage

    3. Cover at least these cases:
       - lifecycle payload shape for `connection_invalidated`
       - silent vs eventful source reload behavior
       - `source_reload_failed` emission only for user-driven failures
       - committed-mutation / reload-failed partial failure emits `connection_invalidated` first, then `source_reload_failed`
       - bootstrap snapshot on mount
       - connection-only root rendering with source metadata visible
       - `a`/`e`/`dd`/`t`/`<C-CR>` connection-first targeting and warn/no-op rules
       - the secondary source-file edit path remains reachable from a connection row through the chosen additive action surface, and that surface stays documented in drawer help text without reviving source rows in the tree
       - `t` fail-closed behavior preserving current connection
       - `perform_action` close-only vs refresh-after-action ownership
       - `current_connection_changed` presentation-only drawer behavior
       - D-31 filter snapshot restore regression after lifecycle events and drawer actions

    4. Emit the lifecycle marker set and `DCFG01_DRAWER_LIFECYCLE_ALL_PASS=true`.
  </action>
  <verify>
    <automated>
      sh -c 'out=$(nvim --headless -u NONE -i NONE -n --cmd "set rtp+=$(pwd)" -c "luafile ci/headless/check_connection_lifecycle.lua" 2>&1); printf "%s\n" "$out" | grep "^DCFG01_DRAWER_LIFECYCLE_ALL_PASS=true$" && printf "%s\n" "$out" | grep "^DCFG01_INVALIDATION_PAYLOAD_OK=true$" && printf "%s\n" "$out" | grep "^DCFG01_SILENT_RELOAD_OK=true$" && printf "%s\n" "$out" | grep "^DCFG01_FAILURE_EVENT_OK=true$" && printf "%s\n" "$out" | grep "^DCFG01_PARTIAL_FAILURE_OK=true$" && printf "%s\n" "$out" | grep "^DCFG01_BOOTSTRAP_SNAPSHOT_OK=true$" && printf "%s\n" "$out" | grep "^DCFG01_CONNECTION_ONLY_ROOT_OK=true$" && printf "%s\n" "$out" | grep "^DCFG01_ACTION_TARGETING_OK=true$" && printf "%s\n" "$out" | grep "^DCFG01_SOURCE_EDIT_REACHABLE_OK=true$" && printf "%s\n" "$out" | grep "^DCFG01_TEST_FAIL_CLOSED_OK=true$" && printf "%s\n" "$out" | grep "^DCFG01_REFRESH_MODE_OK=true$" && printf "%s\n" "$out" | grep "^DCFG01_CURRENT_CONN_VISUAL_OK=true$" && printf "%s\n" "$out" | grep "^DCFG01_PHASE6_FILTER_REGRESSION_OK=true$" && ! printf "%s\n" "$out" | grep -E "FAIL=|Lua error|Traceback"'
    </automated>
  </verify>
  <acceptance_criteria>
    - Headless tests prove the canonical lifecycle payload and silent/eventful split.
    - Headless tests prove D-83 partial-failure ordering for committed mutation plus reload failure.
    - Headless tests prove the drawer root is connection-only and action targeting follows D-68.
    - Headless tests prove secondary source-file editing remains reachable from a connection row without reviving source rows.
    - Headless tests prove `t` is fail-closed and `current_connection_changed` is presentation-only in the drawer.
    - Headless tests prove D-31 filter restore still holds after the drawer rewrite.
  </acceptance_criteria>
  <done>Phase 7 now has dedicated machine-readable proof for the lifecycle surface and the connection-only drawer UX, including regression coverage for the Phase 4 filter contract.</done>
</task>

<task type="auto">
  <id>07-04-02</id>
  <title>Add coordination, reconnect, and async database-switch coverage and wire Phase 7 suites into CI</title>
  <files>ci/headless/check_connection_coordination.lua, .github/workflows/test.yml</files>
  <read_first>
    - .planning/phases/07-connection-only-drawer/07-CONTEXT.md
    - .planning/phases/07-connection-only-drawer/07-VALIDATION.md
    - ci/headless/check_structure_lazy.lua
    - ci/headless/check_connection_lifecycle.lua
    - lua/dbee/handler/init.lua
    - lua/dbee/lsp/init.lua
    - lua/dbee/reconnect.lua
    - lua/dbee/ui/drawer/init.lua
  </read_first>
  <action>
    Add the coordination proof and keep existing Phase 6 coverage live in CI.

    1. Create `ci/headless/check_connection_coordination.lua`.

    2. Cover at least these cases:
       - bootstrap race: invalidation fires after listener registration but before snapshot completes, and replay applies only when `event.authoritative_root_epoch > snapshot_authoritative_epoch[conn_id]`
       - ordinary bootstrap post-snapshot window: after snapshot completion but before ordinary drain completes, inject one invalidation before drain completes and another during drain; both events must be either absorbed by the snapshot or applied via bootstrap drain, and no event in that window may be silently lost
       - bootstrap promotion tail window: after the consumer's last ordinary drain read but before `handler:promote_to_live()` returns, inject an invalidation and assert it is delivered either through the returned tail or through the live listener path after handler-owned promotion flips the consumer live; it must never be stranded in a destroyed bootstrap generation
       - bootstrap overflow: 65+ invalidations land before snapshot completion, warning is emitted, handler rolls immediately to bootstrap generation `N+1`, a fresh snapshot is requested, and at least one invalidation arriving during the resnapshot window is either absorbed by the fresh snapshot or replayed from generation `N+1`; no event in that handoff window is silently lost
       - bootstrap overflow storm: four consecutive `65+` bursts trigger the defensive `bootstrap_overflow_storm` hard-error on the fourth overflow and force consumer teardown/reinitialize
       - handler-owned root single-flight prevents duplicate same-key root requests
       - waiter fanout preserves drawer request fences and LSP compatibility
       - eventful invalidation supersedes the older same-connection flight and old waiters receive `{ error_kind = "superseded", new_epoch }`
       - waiter cleanup removes a torn-down consumer from in-flight waiter lists
       - invalidation bursts batch and rerender once per batch
       - cold survivors stay lazy while visible warm survivors/current LSP connection may rewarm
       - sticky logical-connection retention never auto-selects unrelated survivors
       - silent reconnect rewrite migrates root continuity and in-flight coordination to the new conn_id, keeps the targeted visible subtree warm, prunes old-conn branch state, and preserves the authoritative epoch across the rewrite
       - async `database_switch` shows placeholder first and patches only the targeted row/subtree on completion
       - manual `R` after placeholder display clears the pending placeholder before stale completion arrives, leaves no dead row visible, and drops the stale payload silently
       - `database_selected` after placeholder display does the same
       - eventful source CRUD success while the placeholder is visible does the same for the affected retired and new connection IDs
       - eventful `source_reload` while the placeholder is visible does the same for the affected connection IDs
       - reconnect rewrite while the placeholder is visible keeps the authoritative epoch unchanged, migrates the placeholder only when the rewritten connection identity still matches, otherwise clears it through `conn_id` token mismatch, and still drops stale payloads silently
       - Phase 6 structure-lazy regression guard remains green after Phase 7 coordination changes

    3. Update `.github/workflows/test.yml`.
       - keep Phase 6 suites
       - add `check_connection_lifecycle.lua`
       - add `check_connection_coordination.lua`

    4. Emit the coordination marker set and `DCFG01_COORDINATION_ALL_PASS=true`.
  </action>
  <verify>
    <automated>
      sh -c 'out=$(nvim --headless -u NONE -i NONE -n --cmd "set rtp+=$(pwd)" -c "luafile ci/headless/check_connection_coordination.lua" 2>&1); printf "%s\n" "$out" | grep "^DCFG01_COORDINATION_ALL_PASS=true$" && printf "%s\n" "$out" | grep "^DCFG01_SINGLE_FLIGHT_OK=true$" && printf "%s\n" "$out" | grep "^DCFG01_WAITER_FANOUT_OK=true$" && printf "%s\n" "$out" | grep "^DCFG01_BOOTSTRAP_REPLAY_OK=true$" && printf "%s\n" "$out" | grep "^LIFECYCLE01_BOOTSTRAP_POST_SNAPSHOT_OK=true$" && printf "%s\n" "$out" | grep "^LIFECYCLE01_BOOTSTRAP_TAIL_OK=true$" && printf "%s\n" "$out" | grep "^LIFECYCLE01_BOOTSTRAP_OVERFLOW_OK=true$" && printf "%s\n" "$out" | grep "^LIFECYCLE01_BOOTSTRAP_OVERFLOW_STORM_OK=true$" && printf "%s\n" "$out" | grep "^DCFG01_SUPERSEDED_FLIGHT_OK=true$" && printf "%s\n" "$out" | grep "^DCFG01_WAITER_CLEANUP_OK=true$" && printf "%s\n" "$out" | grep "^DCFG01_BACKPRESSURE_OK=true$" && printf "%s\n" "$out" | grep "^DCFG01_STICKY_SELECTION_OK=true$" && printf "%s\n" "$out" | grep "^DCFG01_RECONNECT_CONTINUITY_OK=true$" && printf "%s\n" "$out" | grep "^DCFG01_DATABASE_SWITCH_ASYNC_OK=true$" && printf "%s\n" "$out" | grep "^DCFG01_DATABASE_SWITCH_STALE_DROP_OK=true$" && printf "%s\n" "$out" | grep "^DCFG01_PHASE6_STRUCTURE_REGRESSION_OK=true$" && ! printf "%s\n" "$out" | grep -E "FAIL=|Lua error|Traceback" && grep -q "check_connection_lifecycle.lua" .github/workflows/test.yml && grep -q "check_connection_coordination.lua" .github/workflows/test.yml && grep -q "check_structure_lazy.lua" .github/workflows/test.yml && grep -q "check_notes_picker.lua" .github/workflows/test.yml && grep -q "check_drawer_filter.lua" .github/workflows/test.yml'
    </automated>
  </verify>
  <acceptance_criteria>
    - Headless tests prove single-flight, bootstrap replay ordering, no-drop ordinary bootstrap handoff, handler-owned tail-safe promotion to live mode, zero-drop-window bootstrap overflow recovery, overflow-storm hard-stop behavior, supersession, waiter cleanup, backpressure, sticky selection, reconnect continuity, and async `database_switch`.
    - Headless tests prove pending `database_switch` placeholders clear on every epoch-bumping authoritative invalidation path, reconnect rewrites preserve same-epoch token semantics, and stale completions never leave dead loading rows visible.
    - Headless tests prove the connection-only drawer coordination changes do not regress Phase 6 structure-lazy behavior.
    - CI runs the new Phase 7 suites and the pre-existing Phase 6 suites together.
  </acceptance_criteria>
  <done>Phase 7 coordination now has dedicated machine-readable proof, and CI runs the new lifecycle/coordination suites alongside the shipped Phase 6 regression checks.</done>
</task>
</tasks>

<verification>
1. `check_connection_lifecycle.lua` passes only if the full lifecycle marker set plus `DCFG01_DRAWER_LIFECYCLE_ALL_PASS=true` is emitted with no `FAIL=` / `Lua error` / `Traceback`.
2. `check_connection_coordination.lua` passes only if the full coordination marker set, including `LIFECYCLE01_BOOTSTRAP_POST_SNAPSHOT_OK=true`, `LIFECYCLE01_BOOTSTRAP_TAIL_OK=true`, `LIFECYCLE01_BOOTSTRAP_OVERFLOW_OK=true`, and `LIFECYCLE01_BOOTSTRAP_OVERFLOW_STORM_OK=true`, plus `DCFG01_COORDINATION_ALL_PASS=true` is emitted with no `FAIL=` / `Lua error` / `Traceback`.
3. `.github/workflows/test.yml` includes `check_connection_lifecycle.lua`, `check_connection_coordination.lua`, `check_structure_lazy.lua`, `check_notes_picker.lua`, and `check_drawer_filter.lua`.
</verification>

<success_criteria>
- Every new Phase 7 lifecycle, drawer, coordination, reconnect, and async database-switch behavior has dedicated headless coverage.
- Phase 4 D-31 and Phase 6 STRUCT-01 / NOTES-01 remain regression-guarded in CI.
- Reviewers can evaluate Phase 7 on marker-based evidence instead of grep-only claims.
</success_criteria>

<output>
After completion, create `.planning/phases/07-connection-only-drawer/07-04-SUMMARY.md`
</output>
