---
phase: 23
plan: 23-02
type: implementation
wave: 2
depends_on:
  - 23-01
files_modified:
  - lua/dbee/api/state.lua
  - lua/dbee/notes_migration.lua
  - lua/dbee/notes_namespace.lua
  - lua/dbee/ui/editor/init.lua
  - lua/dbee/api/ui.lua
  - lua/dbee/ui/drawer/init.lua
  - lua/dbee/ui/drawer/convert.lua
  - lua/dbee.lua
  - plugin/dbee.lua
  - README.md
  - ci/headless/check_folder_scoped_notes.lua
autonomous: true
requirements:
  - One-time migration and folder namespace lifecycle.
requirements_addressed:
  - Migration clone/delete/backup
  - Folder create/delete/rename/move semantics
  - Cleanup and inspect commands
decisions:
  - GN-02
  - GN-03
  - GN-05
  - GN-10
  - GN-12
  - GN-13
  - GN-14
  - GN-15
  - GN-16
  - GN-19
  - GN-20
---

# 23-02 - Migration And Folder Namespace Lifecycle

<objective>
Migrate legacy global notes into folder namespaces once, prevent `global` recreation, and make folder create/delete/rename/move operations maintain note namespace semantics.
</objective>

<threat_model>
- Risk: concurrent nvim instances both migrate and delete/copy the same files. Mitigation: atomic lock directory plus post-lock sentinel recheck.
- Risk: folder deletion permanently deletes notes before folder removal fails. Mitigation: stage namespace directory and restore on handler failure.
- Risk: unsafe folder IDs from manual sidecar edits escape notes root. Mitigation: validate folder IDs before namespace filesystem operations.
- Risk: migration partial failure deletes legacy global. Mitigation: clone first, backup/rename second, sentinel last.
- Risk: bind mounts or symlinks make rename cross filesystem. Mitigation: same-filesystem preflight and fail-closed `EXDEV` handling; no copy+unlink fallback.
- Risk: stale staging/trash dirs leak deleted or partially migrated notes after a crash. Mitigation: startup GC of stale version-coupled scratch dirs before lock acquisition.
- Risk: configured `editor.directory` diverges from migration path. Mitigation: resolve `notes_dir` in `setup_handler()` from the same config/default used by `EditorUI:new()` and pass it into the migration helper.
- Risk: crash mid-promote loses the in-memory rollback list. Mitigation: pre-record `.notes-migration-v1.promote-manifest` once before the first rename with every planned final path, then use it for crash recovery.
- Risk: another nvim instance holds the migration lock while this process initializes Handler/UI. Mitigation: probe lock+sentinel before `m.core_loaded = true` and block handler init while migration is in progress.
</threat_model>

<must_haves>
- Migration is first-launch post-Phase-23 and idempotent.
- Migration clones, not moves, each legacy global note into every existing folder namespace.
- Original `notes/global/` path is removed after successful clone/backup.
- Backup remains available until manual cleanup.
- Backup path is canonical: `notes/global.bak`, or `notes/global.bak.YYYYMMDDHHMMSS` only if the default backup already exists.
- Folder created after sentinel gets an empty namespace only.
- Folder rename never moves a namespace directory.
- Folder delete confirmation mentions note deletion.
- Editor startup never calls `namespace_create_note("global", "welcome")` after Phase 23 migration.
- Raw `"global"` namespace creation is rejected everywhere; drawer global master node is removed in Wave 1.
</must_haves>

<tasks>
<task id="23-02-T1" type="execute">
<read_first>
- `lua/dbee/api/state.lua:35-68`
- `lua/dbee/ui/editor/init.lua:53-177`
- `lua/dbee/ui/editor/init.lua:883-1023`
- `lua/dbee/notes_namespace.lua` if it exists
- `lua/dbee/notes_migration.lua` if it exists
- `lua/dbee/sources.lua:99-132`
- `.planning/phases/23-folder-scoped-notes/23-CONTEXT.md`
</read_first>
<action>
Add folder namespace helpers and Phase 23 migration modules.

Required module boundaries:

1. `lua/dbee/notes_namespace.lua` is the canonical namespace/lifecycle module. It exports:
   - `validate_folder_id(folder_id) -> bool`
   - `is_folder_namespace(ns) -> bool`
   - `parse_folder_namespace(ns) -> folder_id|nil`
   - `folder_namespace_id(folder_id) -> string`, exactly `"folder:" .. folder_id`
   - `folder_namespace_dir(notes_dir, folder_id) -> string`, exactly `notes_dir .. "/" .. folder_namespace_id(folder_id)`
   - `ensure_folder_namespace(notes_dir, folder_id, handler?) -> boolean, err?`
   - `delete_folder_namespace(notes_dir, folder_id, source_id, handler) -> boolean, err?`
   - `create_note_in_folder(editor, notes_dir, handler, folder_id, note_id, content) -> note|nil, err?`
   - `list_existing_folder_namespaces(notes_dir) -> string[]`
   - `recursive_rmdir(path) -> boolean, err?`, implemented as `return vim.fn.delete(path, "rf") == 0`
2. Validate folder IDs before filesystem use. Accept ONLY when `folder_id:match("^folder_[A-Za-z0-9]+$")` is non-nil. The regex IS the gate. NO additional substring blacklist. Reject all other inputs including: empty string, nil, `folder` (no underscore-anything), `Folder_abc` (case-sensitive), `folder_a/b`, `folder_a%00b` (NUL byte), `folder_a.b`, `..`, `folder_-x`.
3. `ensure_folder_namespace(notes_dir, folder_id, handler?)` calls `Handler:list_all_folder_ids_across_sources()` when `handler` is provided. If `error_kind` is returned, refuse before filesystem access. If `folder_id` exists in more than one source, refuse with `vim.notify('dbee: folder_id <id> exists in multiple sources; manual sidecar repair needed', vim.log.levels.ERROR)` before filesystem access.
4. `is_folder_namespace(ns)` returns true only for `"folder:" .. valid_id`; `parse_folder_namespace(ns)` returns the valid folder ID portion or nil. Callers MUST NOT hand-roll `namespace:match("^folder:")`.
5. `create_note_in_folder(...)` validates `folder_id`, calls `ensure_folder_namespace(notes_dir, folder_id, handler)`, then calls `EditorUI:namespace_create_note(namespace, note_id, content, { from_authority = true })`. Raw `EditorUI:namespace_create_note("folder:<id>", ...)` without the authority flag throws; raw `EditorUI:namespace_create_note("global", ...)` always throws `global namespace removed; use <C-g>/<C-l> picker`; local connection namespace creates are unchanged.
6. `EditorUI` exposes thin wrappers for configured directory access, including `editor:delete_folder_namespace(source_id, folder_id)`. It must delegate validation, ensure, delete, create, and list behavior to `notes_namespace`, while owning editor cache clearing after folder delete. Migration imports `notes_namespace` directly and does not instantiate `EditorUI`.
7. `lua/dbee/notes_migration.lua` owns migration orchestration and imports `notes_namespace`. It exports `maybe_run(handler, notes_dir, m)` and pure-read `is_migration_in_progress(notes_dir) -> bool`. Its module header documents the two on-disk manifests (`.notes-migration-v1.promote-manifest`, `.notes-migration-v1.recovery-needed`) and the in-memory `promote_rollback_paths` list.

Required migration:

0a. Ensure `notes_dir` exists BEFORE stale-dir GC or lock probing with `vim.fn.mkdir(notes_dir, "p")`. If mkdir fails, notify `dbee: cannot create notes directory: <path>; check permissions`, throw, and let the fatal latch path handle the process.
0b. Before lock acquisition, scan `notes_dir` for `.notes-migration-v1.staging-<pid>-*` and `.notes-migration-v1.trash-<pid>-*` dirs. For each, parse pid and stat mtime. Keep the dir only when pid is alive (`vim.loop.kill(pid, 0)` succeeds) AND mtime age is <= 1 hour; otherwise remove it with `notes_namespace.recursive_rmdir(path)`, which is pinned to `vim.fn.delete(path, "rf")` and checks the `0` success return code. This intentionally GC's old dirs even if the pid has been reused on a long-uptime system. Emit the count of GC'd dirs to stderr or a debug log.
1. Add a private `lua/dbee/api/state.lua` helper:
   ```lua
   local function _assert_migration_ok()
     if m.migration_fatal_failed then
       error("dbee migration failed; restart nvim to retry. See notes/.notes-migration-v1.last-failure.log for details.")
     end
     if m.migration_in_progress then
       error("another nvim instance is migrating notes; close that instance or restart all nvim instances to retry.")
     end
   end
   ```
   `_assert_migration_ok()` is the FIRST executable statement of `setup_handler()`, `setup_ui()`, `M.handler()`, `M.editor()`, `M.drawer()`, and any future public accessor that reaches handler/editor/drawer.
2. In `setup_handler()` resolve `notes_dir = (m.config.editor and m.config.editor.directory) or (vim.fn.stdpath("state") .. "/dbee/notes")` before setting `m.core_loaded = true`. After the existing `register()` call and BEFORE the current `m.core_loaded = true` line, call `notes_migration.is_migration_in_progress(notes_dir)`. This pure-read helper stats `notes_dir .. "/.notes-migration-v1.lock"` and `notes_dir .. "/.notes-migration-v1"` and returns true iff lock dir exists and sentinel is absent. If true, throw the retryable in-progress message and leave `m.core_loaded=false` and `m.handler=nil` so the next `:Dbee` retries the probe.
3. Migration runs once per session via `notes_migration.maybe_run(handler, notes_dir, m)` in `lua/dbee/notes_migration.lua`. Invoke it at the END of `setup_handler()` in `lua/dbee/api/state.lua:59-68`, AFTER `Handler:new()` returns and BEFORE `setup_handler()` returns to its first caller. Pass the same `notes_dir` resolved in step 2. `EditorUI:new()` at `lua/dbee/api/state.lua:86` must read the same configured path/default. Use `handler:get_sources()` to enumerate sources; `handler.sources` is a map, not an ipairs array.
   - Add `m.migration_fatal_failed` as a separate latch from `m.migration_attempted`.
   - Add `m.migration_in_progress` only for post-probe races or state paths where Handler was already partially initialized; normal pre-handler lock detection throws without setting a sticky latch so the next setup attempt can retry.
   - Wrap `notes_migration.maybe_run(...)` in `pcall` at the end of `setup_handler()`. On any thrown error, set `m.migration_fatal_failed = true`, write full error context/stack and current migration artifact paths to `notes/.notes-migration-v1.last-failure.log` when possible, then rethrow to the caller. This is required because `m.core_loaded` is set before the planned hook in the current file layout.
   - If `maybe_run` returns `false, "lock_held"` despite the pre-handler probe, treat it as a race: set `m.migration_in_progress = true`, clear `m.handler` if it was assigned, and throw the in-progress message. Do not let UI/editor/drawer construction proceed against stale state.
   - Lock-fail/concurrent-instance paths do NOT set `migration_fatal_failed`; only fatal exceptions do.
4. Inside `notes_migration.maybe_run(handler, notes_dir, m)`, the first lines are exactly:
   ```lua
   if m.migration_attempted then return end
   m.migration_attempted = true
   ```
   The flag set MUST happen before any helper I/O, folder enumeration, lock acquisition, or helper-internal pcall. The outer `setup_handler()` pcall still wraps the helper for the fatal latch. A panic mid-migration leaves `migration_attempted = true`, which is correct because the same process must not retry; the user restarts nvim.
   - Before lock acquisition, only verify folder-capable sources and capability shape. A pre-lock `src:load_folders()` call may be used as a precondition probe, but its folder list MUST NOT be used as the migration snapshot because `_ensure_folders_loaded()` can return cached data.
   - After any pre-lock probe, validate every folder-capable source has not landed in `_folders_load_state == "load_failed"`. Any `load_failed` aborts with `vim.notify('dbee: migration aborted — folder source load failed; fix sidecar before retrying', vim.log.levels.WARN)` and no sentinel write. FileSource parse/read errors must set `load_failed` or return an explicit error; they must not silently collapse to `{}` for this migration path.
   - empty folder list under `loaded_ok` proceeds normally per step 5a.
5. Use sentinel path `notes_dir .. "/.notes-migration-v1"`.
6. Use lock directory path `notes_dir .. "/.notes-migration-v1.lock"` and acquire it FIRST with `vim.loop.fs_mkdir(path, 448)`.
   - If mkdir returns EEXIST, `vim.loop.fs_stat(lock_path)` and compute `age_seconds = os.time() - stat.mtime.sec`.
   - If `age_seconds < 0`, treat as fresh lock and fail fast with concurrent-instance message.
   - If `age_seconds < 300`, treat as fresh lock and fail fast with concurrent-instance message.
   - If `age_seconds >= 300`, attempt `vim.loop.fs_rmdir(lock_path)` and retry mkdir once.
6a. Before any staging mkdir, call `vim.loop.fs_statfs(notes_dir)` and `vim.loop.fs_statfs(notes_dir .. '/.notes-migration-v1.staging-precheck-tmp')` after creating the precheck dir as a sibling. Cleanup the precheck dir. If device/filesystem IDs differ, abort with `vim.notify('dbee: migration aborted — cross-filesystem rename detected; not supported. Move notes/ off bind mounts and retry, OR set editor.directory in setup() to a same-filesystem path and retry.', vim.log.levels.ERROR)`.
7. After acquiring lock, re-check sentinel. If sentinel is missing, handle recovery artifacts in this precedence order:
   1. If `notes/.notes-migration-v1.recovery-needed` exists, read the recovery-manifest, verify each listed `final_paths[i]` exists with non-zero size, write the missing sentinel, and delete recovery-needed. If `.notes-migration-v1.promote-manifest` also exists, delete it only if its `migration_run_ts` matches the recovery-manifest `migration_run_ts` OR the recovery-manifest `final_paths` are a superset of the promote-manifest `planned_dst` paths. Otherwise keep both for forensics and notify `dbee: inspect both manifests at notes/.notes-migration-v1.*`. If validation fails, retain recovery-needed, notify, and abort.
   2. Else if `.notes-migration-v1.promote-manifest` exists with `promote_complete = true`, require entry count to equal `expected_count`, verify each `planned_dst` exists with non-zero size and size equals `src_size_bytes`, resume backup/delete/sentinel completion, then delete promote-manifest only after the sentinel succeeds.
   3. Else if `.notes-migration-v1.promote-manifest` exists with `promote_complete = false`, require every `src_staging` to be absent, every `planned_dst` to exist, and each destination size to equal `src_size_bytes`. If all checks pass, treat it as the complete promote case and resume backup/delete/sentinel completion. If any `src_staging` remains or a destination is missing/mismatched, abort with `vim.notify('dbee: promote crashed mid-flight; run :Dbee notes_migration_inspect to inspect notes/.notes-migration-v1.staging-* directories before retry', vim.log.levels.ERROR)` and keep the manifest for forensics.
   4. Else continue with the standard fresh migration path.
7a. AFTER lock acquisition AND AFTER sentinel/recovery-manifest re-check, force a fresh folder snapshot under the lock. For each folder-capable source from `handler:get_sources()`, set `src._folders_load_state = "unloaded"` and call `src:_ensure_folders_loaded()` to re-read from disk. This is the one blessed cache invalidation outside FOLDER15 mutation paths. Then call `Handler:list_all_folder_ids_across_sources()` and inspect `(counts, error_kind)`. If `error_kind` is set or any source remains `load_failed`, abort before namespace creation/backup/delete/sentinel. Folders created by other instances AFTER this post-lock snapshot are intentionally not seeded.
   - If total folder count is zero and `notes/global/` is absent or has zero readable `*.sql` files, write `.notes-migration-v1` and exit cleanly.
   - If total folder count is zero and `notes/global/` has readable `*.sql` files, atomically rename global to the canonical backup BEFORE delete/sentinel. If backup `vim.loop.fs_rename` fails for any reason (including ENOSPC, EACCES, or EXDEV), ABORT migration: do NOT delete `notes/global/`, do NOT write `.notes-migration-v1`, and emit `vim.notify('dbee: backup of legacy global notes failed; migration aborted to prevent data loss', vim.log.levels.ERROR)`. This is different from the staged-promote path because no clones exist; backup IS the data. After successful backup and delete, write `.notes-migration-v1`, emit `vim.notify('dbee: legacy global notes backed up to <backup_path>; create a folder to enable global notes per folder', vim.log.levels.WARN)`, and exit cleanly without clones.
7b. Duplicate folder ID guard:
   - reuse the post-lock `(counts, error_kind)` from step 5a.
   - if `error_kind` is set, abort before namespace creation with the folder-load-failure notification.
   - if any duplicate folder ID appears across sources, abort before namespace creation.
   - emit `vim.notify('dbee: duplicate folder_id detected across sources; migration aborted. Repair sidecars before retry.')`.
   - do NOT write sentinel, delete `notes/global/`, or create backup.
7c. If `notes/global/` exists:
   - collect readable `*.sql` files only.
   - emit `vim.notify('dbee: migrating <N> notes into <F> folders')` before clone.
   - stage all clones into `notes/.notes-migration-v1.staging-<pid>-<random>/folder:<id>/` directories.
   - NO writes go directly to final `notes/folder:<id>/` paths during clone.
   - clone each global SQL file into each staged folder namespace, preserving basename.
   - build the complete pre-record `entries[]` list while staging. Each entry is `{ src_staging, planned_dst, collision_suffix, src_size_bytes }`.
   - if a target filename already exists, preserve existing target and decide the collision-safe suffix during staging, before any promote rename. Format is `<stem>.global-migrated.sql`, then `<stem>.global-migrated.2.sql`, `<stem>.global-migrated.3.sql`, etc.; choose the smallest N >= 2 whose planned destination does not exist.
7d. Verify all staged paths exist, `entries[]` matches the expected file count, every `planned_dst` is collision-resolved, and every `src_size_bytes` matches the staged file size. If verification fails, remove the staging dir with `notes_namespace.recursive_rmdir(path)` and abort migration without sentinel/backup/global delete.
7e. Promote staging per file:
   - For each folder namespace, ensure final dir exists with `vim.loop.fs_mkdir` if missing.
   - Maintain `promote_rollback_paths`, an in-memory list of `planned_dst` values created by this process for rollback on same-process promote failure.
   - Before the first per-file rename, atomically write `notes/.notes-migration-v1.promote-manifest` through tmp+rename with:
     ```json
     {
       "expected_count": 6,
       "promote_complete": false,
       "migration_run_ts": "<ISO-8601>",
       "entries": [
        { "src_staging": "/notes/.notes-migration-v1.staging-123-x/folder:folder_X/welcome.sql", "planned_dst": "/notes/folder:folder_X/welcome.global-migrated.sql", "collision_suffix": ".global-migrated", "src_size_bytes": 128 }
       ]
     }
     ```
   - Do not update or read back the manifest per file. Manifest I/O is two atomic writes total: one pre-record write before first rename, and one rewrite after all renames succeed.
   - Rename each `entry.src_staging` to `entry.planned_dst` with `vim.loop.fs_rename`.
   - Append every successfully-created `planned_dst` to `promote_rollback_paths`.
   - If `fs_rename` returns `EXDEV`, abort with the GN-20 notification and do not attempt copy+unlink fallback.
   - On any non-EXDEV per-file rename failure, rollback by deleting only files in `promote_rollback_paths`. Do NOT delete the entire final namespace dir because it may contain user-created folder notes.
   - After all renames succeed and `#entries == expected_count`, atomically rewrite the promote-manifest with `promote_complete = true`.
8. Backup AFTER successful promotion. Create backup by atomically renaming legacy global dir only to `notes/global.bak`, or `notes/global.bak.YYYYMMDDHHMMSS` if `global.bak` already exists. Use `vim.loop.fs_rename` only; no copy fallback. If backup rename fails, log warning but continue; backup is for safety, not correctness after clones are promoted. This non-fatal rule applies only after staged promotion succeeds, never to the zero-folder-with-content branch.
9. AFTER backup, remove the original `global/` path. If delete fails, log warning. Sentinel still gets written because clones are in place.
10. Write sentinel atomically by writing `<sentinel>.tmp` then renaming. If sentinel write fails, the migration is functionally complete but sentinel-driven idempotency is broken; emit warning and write `notes/.notes-migration-v1.recovery-needed` containing a recovery-manifest JSON. This recovery-manifest is a SEPARATE artifact from the promote-manifest and is written ONLY on sentinel-write failure after promote success:
   ```json
   {
     "folder_ids": ["folder_X", "folder_Y"],
     "final_paths": ["/notes/folder:folder_X/welcome.sql", "/notes/folder:folder_Y/welcome.global-migrated.sql"],
     "backup_path": "notes/global.bak",
     "global_deleted": true,
     "migration_run_ts": "<ISO-8601>",
     "timestamp": "<ISO-8601>"
   }
   ```
   Recovery on next launch runs post-lock and post-sentinel-recheck. It reads the recovery-manifest, verifies each exact `final_paths[i]` file exists with non-zero size, writes `.notes-migration-v1`, then removes the recovery-manifest. If a promote-manifest also exists, delete it only when the manifest timestamps match or the recovery `final_paths` are a superset of promote `planned_dst` paths; otherwise keep both and notify for inspection. If validation fails, log an error, notify the user, and retain the recovery-manifest for intervention.
   After a normal sentinel write succeeds, delete `.notes-migration-v1.promote-manifest` and clear `promote_rollback_paths`.
11. If `notes/global/` does not exist:
   - create current folder namespace dirs.
   - write sentinel.
12. Always release lock on success or failure.
13. Emit completion notification after successful migration.
</action>
<verify>
```bash
rg -n "notes_migration|notes_namespace|folder_namespace|global.bak|.notes-migration-v1|fs_mkdir|fs_statfs|recovery-needed|staging|trash" lua/dbee/api/state.lua lua/dbee/notes_migration.lua lua/dbee/notes_namespace.lua lua/dbee/ui/editor/init.lua
```
</verify>
<acceptance_criteria>
- Migration copies three sample global notes into two sample folders as six files.
- Migration removes original `notes/global/`.
- Migration creates backup.
- Re-running migration does not duplicate files or rewrite folder notes.
- Missing `notes_dir` is created with `vim.fn.mkdir(notes_dir, "p")` before stale-dir GC, and mkdir failure is fatal with a permissions-focused notification.
- Fresh user with `loaded_ok` FileSource, zero folders, and no `notes/global/` writes sentinel and exits cleanly.
- User with legacy `notes/global/` content but zero folders gets backup, `global/` deletion, sentinel, and no clones.
- User with legacy `notes/global/` content but zero folders aborts without deleting `global/` or writing sentinel if backup creation fails.
- Zero-folder legacy backup path emits the user-facing backup notification after successful backup/delete.
- Two simulated migration calls cannot both perform clone/delete work.
- `migration_attempted` is set before any helper I/O and prevents same-process re-entry after an exception.
- Malformed folder IDs do not create paths outside `self.directory`.
- Migration aborts before notes mutation when any folder-capable source did not load successfully.
- Duplicate folder IDs across sources abort migration before namespaces can collide.
- Same-filesystem preflight aborts cross-filesystem staging before destructive work.
- Migration receives the configured `editor.directory` from `setup_handler()` and uses the same default as `EditorUI:new()`.
- `notes_migration.is_migration_in_progress(notes_dir)` is called before `m.core_loaded = true`; lock dir present with sentinel absent throws the in-progress message and leaves `m.core_loaded=false` and `m.handler=nil`.
- `_assert_migration_ok()` is the central latch helper and is the first executable statement at `setup_handler()`, `setup_ui()`, `M.handler()`, `M.editor()`, and `M.drawer()`.
- Migration exception sets `m.migration_fatal_failed`, rethrows, and blocks subsequent `M.editor()`/UI construction with the restart message while leaving `m.editor` nil.
- Fatal migration errors write best-effort details to `notes/.notes-migration-v1.last-failure.log`.
- Pre-lock folder probes do not define the migration snapshot; post-lock reload invalidates folder-capable source caches and uses the disk state under lock.
- The only direct `_folders_load_state = "unloaded"` write outside `sources.lua` is in `lua/dbee/notes_migration.lua`.
- Folder load uncertainty from `Handler:list_all_folder_ids_across_sources()` aborts migration before namespace creation, clone promotion, backup, global delete, or sentinel write.
- Stage verification and per-file promotion rollback remove only migration-created final files on non-EXDEV failure.
- Promote-manifest is pre-recorded once before the first rename with actual `planned_dst` values, `src_staging`, `src_size_bytes`, `expected_count`, and `migration_run_ts`; it flips `promote_complete` only after all renames succeed.
- Recovery with `promote_complete=false` requires every `src_staging` to be absent and every `planned_dst` size to equal `src_size_bytes` before treating promotion as complete.
- Backup failure is warning-only after staged promotion succeeds.
- Backup after staged promotion uses atomic `vim.loop.fs_rename` only.
- Sentinel write failure creates a recovery-manifest with exact final paths and next launch writes the missing sentinel only after final-path validation.
- Recovery-manifest takes precedence over promote-manifest when both exist, but deletes the promote-manifest only when `migration_run_ts` matches or recovery `final_paths` are a superset of promote `planned_dst` paths.
- Stale `.notes-migration-v1.staging-*` and `.notes-migration-v1.trash-*` dirs are GC'd before lock acquisition.
- Marker planned: `GN23_MIGRATION_LOCK_SERIALIZES_OK`.
- Marker planned: `GN23_MIGRATION_IDEMPOTENT_OK`.
- Marker planned: `GN23_FRESH_USER_NO_FOLDERS_PROCEED_OK`.
- Marker planned: `GN23_STARTUP_STALE_DIR_GC_OK`.
- Marker planned: `GN23_MIGRATION_SAME_FS_INVARIANT_OK`.
- Marker planned: `GN23_MIGRATION_CLONES_ALL_FOLDERS_OK`.
- Marker planned: `GN23_MIGRATION_BACKUP_CREATED_OK`.
- Marker planned: `GN23_GLOBAL_DIR_DELETED_OK`.
- Marker planned: `GN23_MIGRATION_PARTIAL_FAILURE_ROLLBACK_OK`.
- Marker planned: `GN23_MIGRATION_PROMOTE_MANIFEST_PRE_RECORD_OK`.
- Marker planned: `GN23_PROMOTE_MANIFEST_PERSISTED_OK`.
- Marker planned: `GN23_RECOVERY_MANIFEST_VALIDATES_FINAL_PATHS_OK`.
- Marker planned: `GN23_BACKUP_FAILURE_FATAL_IN_ZERO_FOLDER_PATH_OK`.
- Marker planned: `GN23_MIGRATION_NOTES_DIR_CONFIG_OK`.
- Marker planned: `GN23_REENTRY_GUARD_FAIL_FAST_OK`.
- Marker planned: `GN23_ZERO_FOLDER_BACKUP_NOTIFY_OK`.
- Marker planned: `GN23_FOLDER_ID_PATH_GUARD_OK`.
- Marker planned: `GN23_MIGRATION_PRECONDITION_LOAD_OK`.
- Marker planned: `GN23_MIGRATION_STAGED_PROMOTE_OK`.
- Marker planned: `GN23_MIGRATION_BACKUP_FAILURE_NON_FATAL_OK`.
- Marker planned: `GN23_MIGRATION_SENTINEL_RECOVERY_OK`.
- Marker planned: `GN23_MIGRATION_FOLDER_SNAPSHOT_POST_LOCK_OK`.
- Marker planned: `GN23_CROSS_SOURCE_FOLDER_ID_GUARD_OK`.
- Marker planned: `GN23_MIGRATION_FATAL_LATCH_BLOCKS_UI_OK`.
- Marker planned: `GN23_FOLDER_RELOAD_UNDER_LOCK_OK`.
- Marker planned: `GN23_FOLDER_LOAD_UNCERTAINTY_FAIL_CLOSED_OK`.
- Marker planned: `GN23_RECOVERY_MANIFEST_PRECEDENCE_OK`.
- Marker planned: `GN23_BACKUP_ATOMIC_RENAME_OK`.
- Marker planned: `GN23_LOCK_IN_PROGRESS_BLOCKS_HANDLER_INIT_OK`.
- Marker planned: `GN23_RECOVERY_VALIDATES_STAGING_ABSENT_AND_SIZE_OK`.
- Marker planned: `GN23_NOTES_DIR_BOOTSTRAP_OK`.
- Marker planned: `GN23_LATCH_FIRST_STATEMENT_AT_ALL_SITES_OK`.
- Marker planned: `GN23_LATCH_CENTRAL_HELPER_GREP_GUARD_OK`.
- Marker planned: `GN23_CACHE_INVALIDATION_GATED_TO_MIGRATION_OK`.
- Marker planned: `GN23_RECOVERY_PROMOTE_MANIFEST_TS_MATCH_OK`.
</acceptance_criteria>
</task>

<task id="23-02-T2" type="execute">
<read_first>
- `lua/dbee/ui/editor/init.lua:157-177`
- `lua/dbee/ui/editor/init.lua:268-281`
- `lua/dbee/ui/editor/init.lua:1214-1242`
- `lua/dbee/ui/editor/init.lua:1305-1314`
</read_first>
<action>
Prevent legacy `global` welcome recreation and select a safe default note.

Required behavior:

1. Remove the fallback that creates `namespace_create_note("global", "welcome")` after Phase 23 migration.
2. Last-note restore:
   - if the saved file is under deleted `notes/global/`, ignore it.
   - if the saved file is under `notes/global.bak/` or `notes/global.bak.YYYYMMDDHHMMSS/`, ignore it.
   - if the saved file is under a valid folder namespace or local namespace and exists, restore it.
3. Default note selection when no last note restores:
   - if current connection has a folder and that folder namespace has notes, use the first sorted folder note.
   - else if any folder namespace has notes, use the first deterministic folder note.
   - else leave `current_note_id` nil.
4. `EditorUI:show()` must handle nil `current_note_id` without error and without creating a note.
5. Empty-buffer behavior when `current_note_id` is nil:
   - open the editor buffer with no note file selected.
   - do not create `welcome.sql`.
   - do not write to any namespace.
   - leave note list/navigation commands no-op or warning-only until a note exists.
6. Keep `create_welcome_note()` only if needed by tests or legacy callers, but it must not be called automatically for Phase 23 startup.
7. Add/update `EditorUI:namespace_create_note` luadoc to document the runtime guard and accepted namespace classes: local connection namespaces may call directly, `folder:*` may call only with `{ from_authority = true }`, and `"global"` is retired and rejected.
</action>
<verify>
```bash
rg -n "create_welcome_note|namespace_create_note\\(\"global\"|current_note_id" lua/dbee/ui/editor/init.lua
```
</verify>
<acceptance_criteria>
- Empty post-migration notes directory does not recreate `notes/global/`.
- Saved last-note path under `global.bak` or `global.bak.YYYYMMDDHHMMSS` is ignored.
- Saved last-note path under `folder:<folder_id>` restores if readable.
- Nil current note does not throw in `EditorUI:show()`.
- Raw `namespace_create_note("global", ...)` throws the retired-global message instead of recreating `notes/global/`.
- Marker planned: `GN23_EDITOR_NO_GLOBAL_WELCOME_RECREATE_OK`.
- Marker planned: `GN23_LAST_NOTE_GLOBAL_BACKUP_IGNORED_OK`.
</acceptance_criteria>
</task>

<task id="23-02-T3" type="execute">
<read_first>
- `lua/dbee/ui/drawer/init.lua:3992-4200`
- `lua/dbee/ui/drawer/convert.lua:476-514`
- `lua/dbee/ui/drawer/init.lua:1010-1045`
- `lua/dbee/handler/init.lua:2657-2692`
- `lua/dbee/ui/editor/init.lua`
- `lua/dbee/notes_namespace.lua`
</read_first>
<action>
Wire folder namespace lifecycle into drawer folder actions.

Required create behavior:

1. After `handler:source_add_folder(source_id, name)` returns `folder_id`, call `editor:ensure_folder_namespace(folder_id)` or `notes_namespace.ensure_folder_namespace(notes_dir, folder_id, handler)`.
2. Folder namespace starts empty. Do not clone backup/global notes after migration.
3. If namespace ensure fails after source folder creation, immediately call `handler:source_remove_folder(source_id, folder_id)` to unwind the source-side folder. If unwind also fails, emit `vim.notify('dbee: add folder failed; folder <id> orphaned in source — manual cleanup needed', vim.log.levels.ERROR)`.
4. Bulk `+ New folder` action (visual-mode multi-select from drawer) uses the same create/ensure/unwind order before the bulk-move RPC:
   - call `handler:source_add_folder(source_id, name)` -> returns `folder_id`.
   - call `editor:ensure_folder_namespace(folder_id)`.
   - on ensure failure, call `handler:source_remove_folder(source_id, folder_id)` to unwind.
   - if unwind also fails, emit `vim.notify('dbee: bulk add failed; folder <id> orphaned in source — manual cleanup needed', vim.log.levels.ERROR)`.
   - abort the bulk operation entirely unless both source add and namespace ensure succeeded.

Required rename behavior:

1. Keep handler rename behavior.
2. Do not move or rename any `folder:<folder_id>` directory.

Required delete behavior:

1. Update confirmation label from `Delete (members ungrouped)` to mention note deletion, for example `Delete folder and notes`.
2. BOTH delete surfaces MUST route through ONE UI entry point: `EditorUI:delete_folder_namespace(source_id, folder_id)`. Drawer, `dbee.actions`, and any future user action call this editor method, not `notes_namespace.delete_folder_namespace(...)` directly. `EditorUI` validates the folder ID through `notes_namespace.validate_folder_id`, delegates filesystem behavior to `notes_namespace.delete_folder_namespace(notes_dir, folder_id, source_id, handler)`, and clears editor namespace cache after successful trash promotion/removal.
3. Required lifecycle helper behavior:
   - before staging the namespace dir to trash, call `Handler:list_all_folder_ids_across_sources()` and verify `folder_id` appears in EXACTLY ONE source.
   - if the helper returns `error_kind`, fail closed with a folder-load-failure notification and abort without staging or calling `source_remove_folder`.
   - if `folder_id` is duplicated across sources, fail closed with `vim.notify('dbee: folder_id <id> duplicated across sources; refusing delete to prevent shared-namespace data loss', vim.log.levels.ERROR)` and abort without staging or calling `source_remove_folder`; the user must repair sidecars first.
   - stage folder dir by renaming `notes/folder:<folder_id>/` to `notes/.notes-migration-v1.trash-<pid>-<random>/folder:<id>/`.
   - if namespace dir does not exist, treat staging as successful no-op.
   - if staging rename returns `EXDEV`, fail closed before source mutation with the GN-20 notification.
   - call `handler:source_remove_folder(source_id, folder_id)` from the lifecycle path.
   - if handler removal succeeds, delete the staged trash dir.
   - after successful trash promotion/removal, `EditorUI:delete_folder_namespace(...)` calls `editor:namespace_clear_cache(notes_namespace.folder_namespace_id(folder_id))`; add this small helper to `EditorUI` if it does not already exist.
   - if handler removal fails, restore the staged namespace directory and show/log the error.
4. Apply the same helper to both:
   - drawer-row action table `delete_folder`
   - bulk-delete action
5. The model/convert layers may pass action metadata and callbacks, but MUST NOT perform direct filesystem operations or call `notes_namespace.delete_folder_namespace(...)` directly. The editor entry point owns UI-facing deletion, and the notes namespace lifecycle helper owns note namespace staging/deletion.

Required move behavior:

1. Do not move note files when a connection moves between folders.
2. The picker namespace changes because `Handler:get_folder_for_connection(conn_id)` returns the new folder.
3. Moving to ungrouped makes picker global namespace nil.
</action>
<verify>
```bash
rg -n "Delete folder and notes|ensure_folder_namespace|delete_folder_namespace|namespace_clear_cache|notes-migration-v1.trash|source_remove_folder" lua/dbee/ui/drawer/init.lua lua/dbee/ui/drawer/convert.lua lua/dbee/ui/editor/init.lua lua/dbee/notes_namespace.lua
```
</verify>
<acceptance_criteria>
- New folder creates `notes/folder:<new-folder-id>/` with zero note files.
- Rename leaves `notes/folder:<folder_id>/` path unchanged.
- Delete folder removes the folder namespace and notes after confirmation.
- Handler delete failure restores the namespace.
- Duplicate folder ID across sources fails closed at delete before staging and before `source_remove_folder`.
- Folder load uncertainty fails closed at delete before staging and before `source_remove_folder`.
- Successful folder delete clears any cached editor namespace state for the folder namespace.
- Moving a connection A -> B does not move files, but picker reads folder B namespace after move.
- Moving a connection to ungrouped makes picker global namespace nil.
- Bulk `+ New folder` creates the source folder, ensures the namespace before bulk move, and unwinds the source folder if namespace creation fails.
- Both delete surfaces use `EditorUI:delete_folder_namespace(...)`; no direct filesystem delete and no direct `notes_namespace.delete_folder_namespace(...)` call appears in convert/model layers.
- Marker planned: `GN23_NEW_FOLDER_NAMESPACE_EMPTY_OK`.
- Marker planned: `GN23_RENAME_FOLDER_NO_NAMESPACE_MOVE_OK`.
- Marker planned: `GN23_DELETE_FOLDER_NAMESPACE_CASCADE_OK`.
- Marker planned: `GN23_DELETE_FOLDER_NAMESPACE_RESTORE_ON_FAIL_OK`.
- Marker planned: `GN23_MOVE_CONN_NAMESPACE_SWITCH_OK`.
- Marker planned: `GN23_FOLDER_DELETE_CASCADE_LIFECYCLE_OK`.
- Marker planned: `GN23_BULK_FOLDER_CREATE_NAMESPACE_OK`.
- Marker planned: `GN23_RUNTIME_CROSS_SOURCE_COLLISION_OK` covers lookup, ensure, and delete surfaces.
- Marker planned: `GN23_FOLDER_LOAD_UNCERTAINTY_FAIL_CLOSED_OK`.
- Marker planned: `GN23_FOLDER_DELETE_NAMESPACE_CACHE_CLEAR_OK`.
- Marker planned: `GN23_DELETE_FOLDER_VIA_EDITOR_ENTRY_OK`.
</acceptance_criteria>
</task>

<task id="23-02-T4" type="execute">
<read_first>
- `lua/dbee.lua`
- `plugin/dbee.lua:8-62`
- `lua/dbee/api/ui.lua`
- `lua/dbee/ui/editor/init.lua`
- `README.md`
</read_first>
<action>
Add manual migration backup cleanup and non-destructive inspection commands.

Required behavior:

1. Add public Lua function `require("dbee").notes_migration_cleanup_backups()`.
2. Add `:Dbee notes_migration_cleanup_backups` subcommand in `plugin/dbee.lua`.
3. Add `notes_migration_cleanup_backups` to the `:Dbee` complete callback so command completion offers it.
4. The cleanup command deletes only Phase 23 backup paths:
   - `notes/global.bak`
   - `notes/global.bak.YYYYMMDDHHMMSS`
   Exact Lua cleanup matchers are anchored to direct backup paths only: `^global%.bak$` and `^global%.bak%.%d%d%d%d%d%d%d%d%d%d%d%d%d%d$`. No backup subdirectory variant is supported.
5. The cleanup command must not delete:
   - `notes/folder:<folder_id>/`
   - local connection namespaces
   - sentinel or lock files unless explicitly stale and safe.
6. Add public Lua function `require("dbee").notes_migration_inspect()`.
7. Add `:Dbee notes_migration_inspect` subcommand and completion entry. The inspect command is pure-read: it lists migration lock state, sentinel presence, staging dirs, trash dirs, recovery-needed manifest summary, promote-manifest summary, last-failure-log content, and staged file counts. It must not delete or mutate any path.
8. Recovery and mid-promote crash notifications must tell users to run `:Dbee notes_migration_inspect`, NOT cleanup.
9. If UI/editor is loaded, both commands use editor's configured notes directory. If not loaded, use configured editor directory if setup is available, otherwise default to `vim.fn.stdpath("state") .. "/dbee/notes"`.
10. Cleanup notifies when no backup exists; inspect notifies when no migration artifacts exist.
11. Documentation: `README.md` gets new entries under the Commands section documenting `:Dbee notes_migration_cleanup_backups` and `:Dbee notes_migration_inspect` with examples, mirroring the style of `wallet_cache_clear` documentation. The Wave 2 execution commit must include the README diff.
</action>
<verify>
```bash
rg -n "notes_migration_cleanup_backups|notes_migration_inspect|global.bak" lua/dbee.lua plugin/dbee.lua lua/dbee/ui/editor/init.lua README.md
```
</verify>
<acceptance_criteria>
- `:Dbee notes_migration_cleanup_backups` is a supported subcommand.
- `:Dbee notes_migration_inspect` is a supported subcommand.
- `:Dbee` command completion lists both migration subcommands.
- Cleanup removes backup paths and leaves folder/local namespaces untouched.
- Cleanup is idempotent when backup is absent.
- Inspect lists lock/sentinel/staging/trash/manifests/last-failure-log state without mutating the filesystem.
- README documents both migration subcommands under Commands with examples.
- Marker planned: `GN23_MIGRATION_CLEANUP_COMMAND_OK`.
- Marker planned: `GN23_NOTES_MIGRATION_INSPECT_COMMAND_OK`.
- Marker planned: `GN23_README_MIGRATION_CLEANUP_DOCUMENTED_OK`.
</acceptance_criteria>
</task>

<task id="23-02-T5" type="execute">
<read_first>
- `ci/headless/check_folder_scoped_notes.lua`
- `lua/dbee/ui/editor/init.lua`
- `lua/dbee/ui/drawer/init.lua`
- `lua/dbee/ui/drawer/convert.lua`
</read_first>
<action>
Add Wave 2 test coverage to `check_folder_scoped_notes.lua`.

Required fixtures:

1. Migration fixture with:
   - `notes/global/cleanup.sql`
   - `notes/global/truncate_individual.sql`
   - `notes/global/welcome.sql`
   - folders `folder_Yrcmu67jeY` and `folder_1JvZNAc5MB`
2. Fresh-user and no-folder fixtures:
   - missing notes root: configured `notes_dir` does not exist before setup -> setup creates the directory with `vim.fn.mkdir(notes_dir, "p")` and then continues to normal fresh-user migration.
   - notes root mkdir failure -> migration raises the permissions-focused fatal error and does not create sentinel or migration artifacts.
   - fresh user: FileSource `_folders_load_state == "loaded_ok"`, `folders = {}`, and no `notes/global/` -> migration writes sentinel and exits cleanly.
   - legacy no-folder user: FileSource `_folders_load_state == "loaded_ok"`, `folders = {}`, and `notes/global/` contains readable SQL -> backup created, `global/` deleted, sentinel written, and no clones.
   - legacy no-folder backup failure: backup creation fails -> migration aborts, `global/` remains, sentinel is absent, and ERROR notify is emitted.
   - legacy no-folder success emits the backup-location warning notify.
3. Migration load precondition fixture:
   - one folder-capable source in `load_failed` state.
   - migration aborts before sentinel, backup, global delete, or clone promotion.
4. Duplicate folder ID fixture:
   - two folder-capable sources expose the same `folder_id`.
   - migration aborts with duplicate-folder notification and no namespace merge.
   - post-migration runtime lookup, namespace ensure, and namespace delete also fail closed for the same duplicated `folder_id`; delete does not stage the namespace or call `source_remove_folder`.
   - corrupt one folder-capable source's folders sidecar so `Handler:list_all_folder_ids_across_sources()` returns `error_kind`; lookup returns `nil, error_kind`, ensure rejects, delete rejects, and migration aborts.
   - `Handler:list_all_folder_ids_across_sources()` recomputes counts per call with no cache.
5. Concurrency fixture:
   - first migration acquires lock.
   - second migration sees lock/sentinel behavior and does not clone/delete.
   - pre-handler probe: lock dir exists and sentinel is absent -> `setup_handler()` throws the in-progress message before `m.core_loaded = true`, leaves `m.core_loaded=false` and `m.handler=nil`, and a later call can retry after the lock disappears.
   - rare race after the pre-handler probe: `maybe_run` returns `false, "lock_held"` -> no UI/editor/drawer construction proceeds against stale state.
   - a folder created after post-lock folder enumeration is intentionally not seeded.
   - EEXIST lock with future-dated mtime is treated as fresh, not stale.
   - EEXIST lock with positive age >= 300 seconds is removed once and mkdir retried once.
   - exception during folder enumeration after the first two lines of `maybe_run` leaves `m.migration_attempted = true`; a second same-process call is a no-op.
   - stub `notes_migration.maybe_run` to throw from the `setup_handler()` hook; assert `m.migration_fatal_failed` is set, `M.editor()` raises `dbee migration failed; restart nvim to retry`, and `m.editor` remains nil.
   - `_assert_migration_ok()` is the first executable statement of `setup_handler()`, `setup_ui()`, `M.handler()`, `M.editor()`, and `M.drawer()`, and inline latch checks outside the helper fail the grep guard.
   - pre-lock-load folders, mutate `folders.json` on disk before post-lock snapshot, and assert the post-lock snapshot matches disk state rather than the pre-lock cache.
   - `_folders_load_state = "unloaded"` appears only in `lua/dbee/notes_migration.lua` and `lua/dbee/sources.lua`.
6. Same-filesystem and stale-dir fixtures:
   - `vim.loop.fs_statfs` mismatch aborts before staging mkdir.
   - simulated `EXDEV` during promote/delete staging aborts with the GN-20 notification and no copy+unlink fallback.
   - stale `.notes-migration-v1.staging-*` and `.notes-migration-v1.trash-*` dirs are removed before lock acquisition when pid is dead or mtime is older than 1 hour.
   - pid-reuse case: pid is alive but scratch dir mtime is older than 1 hour, so the dir is GC'd.
   - stale-dir cleanup uses `vim.fn.delete(path, "rf")` via `notes_namespace.recursive_rmdir(path)`.
7. Failure fixtures:
   - clone/write failure leaves `notes/global/` intact and no sentinel.
   - staged verification failure removes staging and leaves no final namespace.
   - per-file promotion collision preserves existing user file and writes `<stem>.global-migrated.sql`.
   - second and later filename collisions use `<stem>.global-migrated.2.sql`, `<stem>.global-migrated.3.sql`, etc., choosing the smallest available N.
   - mid-promotion non-EXDEV rename failure removes only `promote_rollback_paths` migration-created files and leaves pre-existing folder notes intact.
   - `.notes-migration-v1.promote-manifest` is written once before the first per-file rename and records actual `planned_dst` values after collision suffixing, `src_staging`, `src_size_bytes`, `expected_count`, `migration_run_ts`, and `promote_complete=false`; after all renames succeed it is rewritten once with `promote_complete=true`.
   - no per-file manifest rewrite/read-back occurs during promote.
   - crash-recovery with complete promote-manifest and missing sentinel validates every `planned_dst` with non-zero size and size matching `src_size_bytes` before resuming backup/delete/sentinel completion.
   - crash-recovery with `promote_complete=false` treats promote as complete only when all `src_staging` paths are absent, all `planned_dst` paths exist, and every destination size matches `src_size_bytes`; if any staging path remains or destination check fails, retains the manifest, notifies the user to run `:Dbee notes_migration_inspect`, and does not write the sentinel.
   - if both `.notes-migration-v1.recovery-needed` and `.notes-migration-v1.promote-manifest` exist, recovery-needed takes precedence, writes sentinel only after `final_paths` validation, then deletes promote-manifest only when timestamps match or recovery final paths are a superset of promote planned paths.
   - backup creation failure after staged promotion logs a warning and still writes sentinel.
   - staged-promotion backup uses `vim.loop.fs_rename` only; no copy fallback is called.
   - sentinel write failure creates `.notes-migration-v1.recovery-needed` recovery-manifest with `final_paths`; next launch writes `.notes-migration-v1` only after validating those exact final paths, including collision-suffixed paths.
8. Folder ID path guard fixture:
   - include 8+ negative inputs: empty string, nil, `folder`, `Folder_abc`, `folder_a/b`, `folder_a%00b`, `folder_a.b`, `..`, `folder_-x`.
   - assert the regex helper is the only gate and no rejected input creates a path.
   - `notes_namespace.is_folder_namespace(ns)` and `parse_folder_namespace(ns)` accept only `folder:` plus a valid ID and reject malformed prefix strings.
9. Folder lifecycle fixture:
   - create, rename, delete, move, ungroup.
   - bulk `+ New folder` creates source folder, calls `ensure_folder_namespace` before bulk move, and unwinds the source folder on namespace failure.
   - both drawer-row and bulk-delete actions route through `EditorUI:delete_folder_namespace(...)`, not directly through `notes_namespace.delete_folder_namespace(...)`.
   - raw `EditorUI:namespace_create_note("folder:<id>", ...)` without authority flag errors; `<C-g>` folder create succeeds only through `notes_namespace.create_note_in_folder(...)`.
   - raw `EditorUI:namespace_create_note("global", ...)` errors with `global namespace removed; use <C-g>/<C-l> picker`.
   - successful folder delete clears editor namespace cache for the folder namespace.
10. Cleanup command fixture:
   - backup deleted, folder/local namespaces untouched.
   - anchored cleanup matchers delete only `global.bak` and `global.bak.YYYYMMDDHHMMSS`.
   - `:Dbee` completion callback includes `notes_migration_cleanup_backups` and `notes_migration_inspect`.
   - `notes_migration_inspect` lists lock, sentinel, staging/trash dirs, recovery-needed, promote-manifest, last-failure-log, and staged file counts without deleting anything.
   - README documents `:Dbee notes_migration_cleanup_backups` and `:Dbee notes_migration_inspect` under Commands with examples.
11. Configured notes directory fixture:
   - setup uses custom `editor.directory`.
   - migration receives that path from `setup_handler()` and migrates legacy `global/` there, not under the default state directory.
   - `EditorUI:new()` observes the same configured path/default expression.
12. Read-path performance fixture:
   - 100 sequential calls to `Handler:get_folder_for_connection()` issue zero additional `_ensure_folders_loaded()` reload-from-disk operations after the initial memoized load.
13. Error-kind vocabulary fixture:
   - `error_kind` is nil or `"load_failed"` only; any other non-comment enum value fails the plan gate.
14. Diagnostic migration perf fixture:
   - 100 legacy notes x 10 folders fixture runs `notes_migration.maybe_run` headlessly.
   - Emit `GN23_MIGRATION_PERF_BUDGET_DIAGNOSTIC` with P95 timing and target <= 250ms on local SSD. This marker is diagnostic and not counted in `GN23_STRICT_MARKER_COUNT`.
</action>
<verify>
```bash
nvim --headless -u NONE -i NONE -n --cmd "set rtp+=$(pwd)" -c "luafile ci/headless/check_folder_scoped_notes.lua"
```
</verify>
<acceptance_criteria>
- Wave 2 tests emit migration/lifecycle/path markers from top-level PLAN.md, including all r1/r2 review additions assigned to Wave 2.
- Test fixture confirms exactly six migrated folder note files for the provided 3-note x 2-folder state.
- Test fixture confirms no `notes/global/` directory after successful migration.
- Test fixture confirms the r3/r4/r5 markers for promote-manifest persistence/pre-recording, final-path recovery validation, recovery precedence, zero-folder backup fatality, configured notes-dir migration, fatal latch, post-lock reload, load-uncertainty fail-closed behavior, folder-note creation authority, atomic backup rename, README documentation, zero-folder backup notification, read-path no-invalidate behavior, folder-delete cache clearing, lock-in-progress handler blocking, notes-dir bootstrap, central latch helper/order, cache invalidation gating, stronger recovery validation, migration inspect command, editor-owned delete entry, error-kind vocab lock, and recovery/promote manifest correlation.
</acceptance_criteria>
</task>
</tasks>

<verification>
Run the `check_folder_scoped_notes.lua` headless command after implementation.
</verification>

<success_criteria>
- Migration safely clones legacy global notes into current folder namespaces exactly once.
- Folder namespace lifecycle follows create/rename/delete/move semantics.
- Backup cleanup is manual and scoped only to migration backups.
- Editor startup never resurrects installation-wide global notes.
- Fresh users with zero folders complete migration idempotently instead of entering an abort loop.
</success_criteria>
