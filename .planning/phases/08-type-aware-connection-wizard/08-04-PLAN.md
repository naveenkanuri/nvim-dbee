---
phase: 08-type-aware-connection-wizard
plan: 04
type: execute
wave: 4
depends_on: ["08-01", "08-02", "08-03"]
files_modified:
  - ci/headless/check_connection_wizard.lua
  - ci/headless/check_filesource_persistence.lua
  - .github/workflows/test.yml
autonomous: true
requirements: [DCFG-02]
---

<objective>
Add machine-readable proof for the wizard surface, transient-spec save gate, and FileSource hardening.

Purpose: Phase 8 is only ship-ready if its new UI and persistence contracts are proven through real DrawerUI paths and temp-file-backed FileSource tests. By the end of this plan, the wizard flow, metadata-first edit seeding, pre-save ping, atomic persistence, and round-trip preservation all have dedicated automated evidence, and the relevant Phase 7 regressions still run in CI.
</objective>

<execution_context>
@/Users/naveenkanuri/Documents/nvim-dbee/.codex/skills/gsd-plan-phase/SKILL.md
@/Users/naveenkanuri/Documents/nvim-dbee/.codex/get-shit-done/workflows/plan-phase.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-connection-only-drawer/07-CONTEXT.md
@.planning/phases/08-type-aware-connection-wizard/08-CONTEXT.md
@.planning/phases/08-type-aware-connection-wizard/08-RESEARCH.md
</context>

<must_haves>
  <truths>
    - "Phase 8 wizard claims require headless proof. Use real DrawerUI entry points, real handler wrappers, and temp-file-backed FileSource fixtures wherever possible; stub only the genuinely external ping transport."
    - "FileSource hardening claims require real filesystem fixtures. Atomic write and field-preservation behavior must be exercised against temp JSON files, not a pure table stub."
    - "Phase 7 lifecycle guarantees remain the substrate. Phase 8 coverage must keep relevant Phase 7 lifecycle and drawer regression suites in CI instead of replacing them."
    - "Headless proof must cover wallet alias discovery/fallback, the FileSource-only scoped-mode metadata boundary including stale `wizard` deletion, the unified `convert.lua` edit seam, `Other`-mode ping gating, the nil-current no-auto-activate case, and D-105 password placeholder pass-through. Those are not manual-only claims."
    - "The proof is split into one wizard/integration suite and one FileSource persistence suite so failures isolate cleanly."
  </truths>
  <artifacts>
    - path: "ci/headless/check_connection_wizard.lua"
      provides: "headless coverage for wizard flow, metadata-first edit seeding, transient-spec ping gating, raw fallback, and fail-closed drawer behavior"
      contains: "DCFG02_WIZARD_ALL_PASS"
    - path: "ci/headless/check_filesource_persistence.lua"
      provides: "temp-file-backed coverage for atomic writes, unknown-field preservation, and round-trip metadata fidelity"
      contains: "DCFG02_FILESOURCE_ALL_PASS"
    - path: ".github/workflows/test.yml"
      provides: "CI execution of Phase 8 suites alongside the relevant Phase 7 regressions"
      contains: "check_connection_wizard.lua"
  </artifacts>
  <key_links>
    - from: "ci/headless/check_connection_wizard.lua"
      to: "lua/dbee/ui/drawer/init.lua"
      via: "the suite drives real add/edit drawer paths instead of bypassing them with direct serializer calls"
      pattern: "DrawerUI"
    - from: "ci/headless/check_filesource_persistence.lua"
      to: "lua/dbee/sources.lua"
      via: "the suite validates the atomic write and metadata preservation contract through real temp files"
      pattern: "FileSource"
  </key_links>
</must_haves>

<constraints>
- Honor Phase 4 D-31, Phase 7 D-67 through D-69, D-82 through D-83, and Phase 8 D-89 through D-106 verbatim.
- Use real temp files for FileSource persistence tests.
- Wizard headless coverage must drive real drawer entry points and the real wizard surface where possible; do not replace add/edit routing with direct source mutation stubs or a separate raw-prompt save path in the cases claiming drawer integration.
- Wallet alias discovery/fallback, scoped-mode metadata stripping, `Other`-mode ping gating, nil-current no-auto-activate, nested wizard metadata preservation, and convert-row edit delegation require automated markers rather than manual-only sign-off.
- Keep Phase 7 regression suites in CI: `check_connection_lifecycle.lua`, `check_connection_coordination.lua`, and `check_drawer_filter.lua`.
- The new suites should emit marker-based evidence and fail cleanly on `FAIL=`, `Lua error`, or `Traceback`.
</constraints>

<tasks>
<task type="auto">
  <id>08-04-01</id>
  <title>Add headless coverage for wizard flow and transient-spec save gating</title>
  <files>ci/headless/check_connection_wizard.lua</files>
  <read_first>
    - .planning/phases/08-type-aware-connection-wizard/08-CONTEXT.md
    - ci/headless/check_connection_lifecycle.lua
    - ci/headless/check_connection_coordination.lua
    - lua/dbee/ui/drawer/init.lua
  </read_first>
  <action>
    Add one suite that proves the user-visible wizard behavior through drawer entry points.

    1. Drive add/edit through real `DrawerUI` actions.
       - source preselection / picker behavior
       - wizard open path
       - searchable/filter edit path through `lua/dbee/ui/drawer/convert.lua`
       - preserved secondary source-file edit path
       - unsupported or non-scoped rows open directly into `Other` mode instead of a raw prompt

    2. Prove mode and seed behavior.
       - metadata-first edit seed
       - lossless parse fallback
       - raw compatibility fallback
       - Postgres URL unsupported-query-parameter fallback
       - wallet alias discovery from both wallet-directory and wallet-`.zip` fixtures populates a selectable dropdown
       - broken or missing `tnsnames.ora` keeps manual alias entry available and surfaces a warning

    3. Prove submit gating.
       - local validation blocks empty/invalid state
       - transient-spec ping runs before save on every mode, including `Other`
       - ping failure aborts save and preserves current connection
       - forced ping failure in `Other` mode results in no `source_add_connection()` / `source_update_connection()` mutation
       - success calls the shared wizard submit dispatcher and then the existing source mutation wrapper
       - FileSource-backed scoped-mode submit persists the `wizard` metadata block
       - FileSource raw compatibility edit on a row with pre-existing `wizard` metadata removes the `wizard` block from persisted JSON
       - all raw compatibility submits strip the `wizard` metadata block, including non-FileSource and FileSource `Other` or raw-fallback cases
       - partial failure still follows D-83
       - no auto-activate on success, including the nil-current-with-existing-connections case
       - when current starts nil and existing connections already exist, save restores nil instead of leaving the handler's auto-selected current in place, and the captured `connection_invalidated.current_conn_id_after` is nil on both success and partial failure

    4. Emit marker-based proof.
      - `DCFG02_WIZARD_ALL_PASS=true`
      - `DCFG02_SOURCE_PICKER_OK=true`
      - `DCFG02_MODE_FLOW_OK=true`
      - `DCFG02_WALLET_ALIAS_DISCOVERY_OK=true`
      - `DCFG02_WALLET_ALIAS_FALLBACK_OK=true`
      - `DCFG02_OTHER_FALLBACK_OK=true`
      - `DCFG02_LOCAL_VALIDATION_OK=true`
      - `DCFG02_TRANSIENT_PING_OK=true`
      - `DCFG02_OTHER_MODE_PING_GATED_OK=true`
      - `DCFG02_FAIL_CLOSED_CURRENT_OK=true`
      - `DCFG02_EDIT_SEED_METADATA_OK=true`
      - `DCFG02_EDIT_PARSE_FALLBACK_OK=true`
      - `DCFG02_NON_FILESOURCE_NO_METADATA_OK=true`
      - `DCFG02_FILESOURCE_RAW_FALLBACK_NO_METADATA_OK=true`
      - `DCFG02_EDIT_SEAM_CONSISTENT_OK=true`
      - `DCFG02_PG_URL_UNSUPPORTED_FALLBACK_OK=true`
      - `DCFG02_D83_PARTIAL_FAILURE_OK=true`
      - `DCFG02_NO_AUTO_ACTIVATE_OK=true`
      - `DCFG02_SOURCE_EDIT_SECONDARY_OK=true`
  </action>
  <verify>
    <automated>
      sh -c 'out=$(nvim --headless -u NONE -i NONE -n --cmd "set rtp+=$(pwd)" -c "luafile ci/headless/check_connection_wizard.lua" 2>&1); printf "%s\n" "$out" | grep "^DCFG02_WIZARD_ALL_PASS=true$" && printf "%s\n" "$out" | grep "^DCFG02_SOURCE_PICKER_OK=true$" && printf "%s\n" "$out" | grep "^DCFG02_MODE_FLOW_OK=true$" && printf "%s\n" "$out" | grep "^DCFG02_WALLET_ALIAS_DISCOVERY_OK=true$" && printf "%s\n" "$out" | grep "^DCFG02_WALLET_ALIAS_FALLBACK_OK=true$" && printf "%s\n" "$out" | grep "^DCFG02_OTHER_FALLBACK_OK=true$" && printf "%s\n" "$out" | grep "^DCFG02_LOCAL_VALIDATION_OK=true$" && printf "%s\n" "$out" | grep "^DCFG02_TRANSIENT_PING_OK=true$" && printf "%s\n" "$out" | grep "^DCFG02_OTHER_MODE_PING_GATED_OK=true$" && printf "%s\n" "$out" | grep "^DCFG02_FAIL_CLOSED_CURRENT_OK=true$" && printf "%s\n" "$out" | grep "^DCFG02_EDIT_SEED_METADATA_OK=true$" && printf "%s\n" "$out" | grep "^DCFG02_EDIT_PARSE_FALLBACK_OK=true$" && printf "%s\n" "$out" | grep "^DCFG02_NON_FILESOURCE_NO_METADATA_OK=true$" && printf "%s\n" "$out" | grep "^DCFG02_FILESOURCE_RAW_FALLBACK_NO_METADATA_OK=true$" && printf "%s\n" "$out" | grep "^DCFG02_EDIT_SEAM_CONSISTENT_OK=true$" && printf "%s\n" "$out" | grep "^DCFG02_PG_URL_UNSUPPORTED_FALLBACK_OK=true$" && printf "%s\n" "$out" | grep "^DCFG02_D83_PARTIAL_FAILURE_OK=true$" && printf "%s\n" "$out" | grep "^DCFG02_NO_AUTO_ACTIVATE_OK=true$" && printf "%s\n" "$out" | grep "^DCFG02_SOURCE_EDIT_SECONDARY_OK=true$" && ! printf "%s\n" "$out" | grep -E "FAIL=|Lua error|Traceback"'
    </automated>
  </verify>
  <acceptance_criteria>
    - The wizard flow is proven through real drawer add/edit entry points, including `Other`-mode compatibility paths.
    - Wallet alias discovery is proven for both wallet-directory and wallet-`.zip` fixtures, and broken wallets fall back to manual alias entry with warning.
    - Edit seeding prefers metadata, then lossless parse, then raw fallback.
    - `convert.lua` searchable/filter edit paths delegate to the same wizard-backed loader as the primary drawer actions.
    - Ping failure is fail closed for every mode, including `Other`, and success does not auto-activate, including when current starts nil and existing connections already exist.
    - FileSource-backed scoped-mode submit persists `wizard` metadata, while FileSource raw compatibility edits physically remove stale `wizard` metadata and all other raw compatibility submits strip it before source mutation.
    - Nil-current preservation is proven at both the final state and event level: success and D-83 partial failure both keep `current = nil` and emit `current_conn_id_after = nil` when nil current was intentionally preserved.
    - Secondary source-file editing remains reachable.
  </acceptance_criteria>
  <done>Phase 8 now has machine-readable proof for the user-visible wizard flow and pre-save gate.</done>
</task>

<task type="auto">
  <id>08-04-02</id>
  <title>Add temp-file FileSource coverage and wire Phase 8 plus Phase 7 regressions into CI</title>
  <files>ci/headless/check_filesource_persistence.lua, .github/workflows/test.yml</files>
  <read_first>
    - .planning/phases/08-type-aware-connection-wizard/08-CONTEXT.md
    - lua/dbee/sources.lua
    - .github/workflows/test.yml
  </read_first>
  <action>
    Prove the persistence contract and keep the older lifecycle substrate guarded.

    1. Add temp-file-backed FileSource coverage.
       - atomic write leaves original file untouched on failure
       - unknown sibling records survive
       - unknown fields on the edited record survive
       - untouched nested keys inside additive `wizard` metadata survive when known wizard leaves are edited
       - wizard metadata round-trips intact
       - Postgres URL exact string round-trips
       - Postgres Form fields plus rendered URL round-trip
       - Oracle descriptor raw text round-trips
       - cleartext and env-placeholder password inputs round-trip byte-for-byte without auto-expansion

    2. Emit marker-based proof.
       - `DCFG02_FILESOURCE_ALL_PASS=true`
       - `DCFG02_ATOMIC_WRITE_OK=true`
       - `DCFG02_UNKNOWN_FIELD_PRESERVE_OK=true`
       - `DCFG02_WIZARD_NESTED_PRESERVE_OK=true`
       - `DCFG02_METADATA_ROUNDTRIP_OK=true`
       - `DCFG02_PASSWORD_PLACEHOLDER_PRESERVED_OK=true`
       - `DCFG02_PG_URL_ROUNDTRIP_OK=true`
       - `DCFG02_PG_FORM_RENDERED_URL_OK=true`
       - `DCFG02_ORACLE_DESCRIPTOR_ROUNDTRIP_OK=true`
       - `DCFG02_DELETE_PRESERVES_SIBLINGS_OK=true`

    3. Update CI.
       - add `check_connection_wizard.lua`
       - add `check_filesource_persistence.lua`
       - keep `check_connection_lifecycle.lua`
       - keep `check_connection_coordination.lua`
       - keep `check_drawer_filter.lua`
  </action>
  <verify>
    <automated>
      sh -c 'out=$(nvim --headless -u NONE -i NONE -n --cmd "set rtp+=$(pwd)" -c "luafile ci/headless/check_filesource_persistence.lua" 2>&1); printf "%s\n" "$out" | grep "^DCFG02_FILESOURCE_ALL_PASS=true$" && printf "%s\n" "$out" | grep "^DCFG02_ATOMIC_WRITE_OK=true$" && printf "%s\n" "$out" | grep "^DCFG02_UNKNOWN_FIELD_PRESERVE_OK=true$" && printf "%s\n" "$out" | grep "^DCFG02_WIZARD_NESTED_PRESERVE_OK=true$" && printf "%s\n" "$out" | grep "^DCFG02_METADATA_ROUNDTRIP_OK=true$" && printf "%s\n" "$out" | grep "^DCFG02_PASSWORD_PLACEHOLDER_PRESERVED_OK=true$" && printf "%s\n" "$out" | grep "^DCFG02_PG_URL_ROUNDTRIP_OK=true$" && printf "%s\n" "$out" | grep "^DCFG02_PG_FORM_RENDERED_URL_OK=true$" && printf "%s\n" "$out" | grep "^DCFG02_ORACLE_DESCRIPTOR_ROUNDTRIP_OK=true$" && printf "%s\n" "$out" | grep "^DCFG02_DELETE_PRESERVES_SIBLINGS_OK=true$" && ! printf "%s\n" "$out" | grep -E "FAIL=|Lua error|Traceback" && grep -q "check_connection_wizard.lua" .github/workflows/test.yml && grep -q "check_filesource_persistence.lua" .github/workflows/test.yml && grep -q "check_connection_lifecycle.lua" .github/workflows/test.yml && grep -q "check_connection_coordination.lua" .github/workflows/test.yml && grep -q "check_drawer_filter.lua" .github/workflows/test.yml'
    </automated>
  </verify>
  <acceptance_criteria>
    - FileSource hardening is proven against real temp files.
    - D-105 password pass-through is proven for both literal cleartext and env-placeholder values.
    - Nested additive wizard metadata survives edits to known wizard leaf fields, including untouched custom keys alongside updated known wizard fields.
    - Per-mode round-trip fidelity is proven for the locked raw/rendered-url cases.
    - CI runs both new Phase 8 suites and the relevant Phase 7 regressions together.
  </acceptance_criteria>
  <done>Phase 8 persistence hardening is now proven, and CI keeps the wizard plus the Phase 7 lifecycle substrate guarded together.</done>
</task>
</tasks>

<verification>
1. `check_connection_wizard.lua` passes only if the full wizard marker set, including wallet alias discovery/fallback, `Other`-mode ping gating, scoped metadata stripping with pre-existing metadata removal, nil-current no-auto-activate proof, and edit-seam consistency, plus `DCFG02_WIZARD_ALL_PASS=true` is emitted with no `FAIL=`, `Lua error`, or `Traceback`.
2. `check_filesource_persistence.lua` passes only if the full persistence marker set, including D-105 password placeholder pass-through and nested wizard metadata preservation, plus `DCFG02_FILESOURCE_ALL_PASS=true` is emitted with no `FAIL=`, `Lua error`, or `Traceback`.
3. `.github/workflows/test.yml` includes the new Phase 8 suites and the relevant Phase 7 regression suites.
</verification>

<success_criteria>
- Phase 8 wizard flow has dedicated headless proof for both typed and `Other`-mode compatibility saves.
- FileSource hardening has dedicated temp-file-backed proof.
- CI keeps Phase 8 evidence and Phase 7 regressions together.
</success_criteria>

<output>
After completion, create `.planning/phases/08-type-aware-connection-wizard/08-04-SUMMARY.md`
</output>
