---
phase: 06-structure-laziness-notes-picker
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dbee/endpoints.go
  - dbee/handler/handler.go
  - dbee/handler/event_bus.go
  - lua/dbee/api/__register.lua
  - lua/dbee/config.lua
  - lua/dbee/handler/init.lua
  - lua/dbee/doc.lua
  - lua/dbee/ui/drawer/init.lua
  - lua/dbee/ui/drawer/convert.lua
  - lua/dbee/ui/drawer/model.lua
  - ci/headless/check_structure_lazy.lua
  - .github/workflows/test.yml
autonomous: true
requirements: [STRUCT-01]

must_haves:
  truths:
    - "Expanding a connection shows a placeholder child immediately and schedules at most one drawer-owned async full-tree warmup for that connection until an explicit same-connection drawer action clears or replaces the cached root"
    - "Once a connection's full-tree structure is cached, expanding schemas and other cached container branches materializes only the next level from authoritative cached data instead of recursively hydrating the whole subtree during refresh"
    - "Table-like expansion is truthfully non-blocking: it never calls synchronous `connection_get_columns()` from the expansion path and instead uses an additive async child-fetch flow keyed by branch identity and request generation, carrying `materialization = struct.type` end-to-end so tables and views keep the existing truthful column semantics"
    - "All new Phase 6 drawer cache state lives in one namespaced container, `self._struct_cache`, with root-level fields (`root`, `root_gen`, `root_applied`, `root_epoch`, `loaded_lazy_ids`) plus per-connection branch state under `branches[conn_id][branch_key]`; root cache entries store `{ structures, error }` and branch cache entries store `{ raw, error, built_count, render_limit, request_gen, applied_gen, loading }`"
    - "All full drawer rebuilds flow through one canonical renderer, `build_tree_from_struct_cache(_struct_cache, ...)`, which is the only Phase 6 path allowed to materialize root structures plus branch-local loaded children, branch errors, `built_count`, `render_limit`, and `Load more...` sentinel state back into tree nodes"
    - "Branch-scoped child request/apply/loading state is keyed by `(branch_id, kind)` inside `self._struct_cache.branches`; for table/view child families, `kind` is canonically `\"columns\"` and `branch_id` is exactly `convert.structure_node_id(parent_id, struct)` so future child kinds cannot collide"
    - "Branch payloads apply only when `payload.request_id == branch.request_gen`, `payload.root_epoch == root_epoch[conn_id]`, and `branch.applied_gen < payload.request_id`; every other branch result is stale and drops silently"
    - "Drawer-owned root apply requires `caller_token == \"drawer\"` plus the drawer-owned pending request token (`root_gen[conn_id]`) plus matching `root_epoch`; legacy one-arg root callers remain valid, but non-drawer payloads never mutate drawer root cache"
    - "Oversized branches cache raw authoritative children, build at most the first 1000 `DrawerUINode`s on initial materialization, and use an inline deterministic `Load more...` sentinel that mutates only the targeted branch through the in-repo tree primitives `tree:remove_node()`, `tree:add_node()`, and `tree:set_nodes()`"
    - "DrawerUI presentation refresh is cache-preserving by default only for the drawer-internal callsites Phase 6 owns: `DrawerUI:refresh()` itself, `show()`, `on_current_note_changed()`, and note-buffer `BufModifiedSet` refresh. Those paths MUST NOT clear `_struct_cache`; only explicit authoritative invalidators (manual `R` and `database_selected`) may drop targeted root/branch state. `current_connection_changed` and `perform_action()` action callback refresh behavior keep current behavior and remain Phase 7 (`DCFG-01`) territory per D-32, D-54, and D-57"
    - "Manual `R` and `database_selected` clear only the targeted connection before any refetch, prune flat `loaded_lazy_ids` through the encoded `ID_SEP` rule, clear saved table/view expansion IDs for that connection, and bump `root_epoch` exactly once during that pre-request clear; manual `R` resolves its owning connection by the D-63 nearest-ancestor rule"
    - "DRAW-01 filter guarantees remain intact: typing stays zero-RPC, search corpora are built only from authoritative root/searchable cached structure data, and Phase 6 `columns` child loads plus `Load more...` invalidate only `cached_render_snapshot`; `cached_search_model` invalidates only on authoritative root replacement or future searchable branch kinds, while active filter views stay frozen until filter close even if underlying `_struct_cache` mutates"
    - "The existing full-tree `structure_loaded` contract remains backward compatible for current consumers such as the drawer and LSP; additive `root_epoch` and `caller_token` must not force old consumers to special-case a different event family"
    - "Phase 6 does not define `connection_invalidated`, `_source_reload_silent`, `source_reload_failed`, public source-lifecycle invalidation, `current_connection_changed`, source-action dispatcher ownership, LSP root single-flight, invalidation backpressure, or late queued startup invalidation semantics; those move to Phase 7 per D-61"
    - "STRUCT-01 does not silently claim the remaining synchronous `connection_list_databases()` seam is fixed; Phase 6 documents it as a residual watchpoint for Phase 7 (`DCFG-01`)"
  artifacts:
    - path: "dbee/endpoints.go"
      provides: "additive async child-fetch endpoint for truthful table/view child loading plus backward-compatible optional drawer-owned root fencing args"
      contains: "DbeeConnectionGetColumnsAsync"
    - path: "dbee/handler/handler.go"
      provides: "goroutine-backed child fetch and backward-compatible full-tree async request plumbing with additive caller-token and root-epoch support for drawer-owned loads"
      contains: "ConnectionGetColumnsAsync"
    - path: "dbee/handler/event_bus.go"
      provides: "Lua event payloads for branch-scoped child results plus additive caller-token and root-epoch echoing on structure_loaded"
      contains: "structure_children_loaded"
    - path: "lua/dbee/api/__register.lua"
      provides: "generated manifest registration for the additive async child-fetch RPC, regenerated from the Go manifest entrypoint after RPC changes"
      contains: "DbeeConnectionGetColumnsAsync"
    - path: "lua/dbee/config.lua"
      provides: "drawer `r` mapping still routed through the in-built refresh action, now documented as D-63 nearest-ancestor target reload semantics rather than global cache clear"
      contains: "action = \"refresh\""
    - path: "lua/dbee/handler/init.lua"
      provides: "Lua wrappers for additive async child fetch plus backward-compatible optional drawer-owned root fencing args"
      contains: "connection_get_columns_async"
    - path: "lua/dbee/doc.lua"
      provides: "reference docs for additive child fetch and drawer-owned root fencing"
      contains: "DbeeConnectionGetColumnsAsync"
    - path: "lua/dbee/ui/drawer/init.lua"
      provides: "single-container structure cache ownership, exact drawer-owned root fencing, explicit root/branch error storage, cache-preserving presentation refresh, nearest-ancestor reload targeting, and bounded branch materialization"
      contains: "_struct_cache"
    - path: "lua/dbee/ui/drawer/convert.lua"
      provides: "stable branch identity, placeholder/error/sentinel row helpers, and raw-child-to-node chunk construction for async table/view children"
      contains: "structure_node_id"
    - path: "lua/dbee/ui/drawer/model.lua"
      provides: "the canonical `build_tree_from_struct_cache(...)` renderer plus authoritative search-model building that ignores placeholder and sentinel rows and never warms cold loaders during filter typing"
      contains: "build_tree_from_struct_cache"
    - path: "ci/headless/check_structure_lazy.lua"
      provides: "headless coverage for lazy materialization, async child loading, explicit error caching, chunking, and drawer-owned root fencing"
      contains: "STRUCT01_ALL_PASS"
    - path: ".github/workflows/test.yml"
      provides: "CI execution of the narrowed Phase 6 structure-lazy headless check"
      contains: "check_structure_lazy.lua"
  key_links:
    - from: "dbee/endpoints.go"
      to: "dbee/handler/handler.go"
      via: "new async child-fetch endpoint delegates to handler goroutine-backed child loading"
      pattern: "ConnectionGetColumnsAsync"
    - from: "lua/dbee/api/__register.lua"
      to: "lua/dbee/handler/init.lua"
      via: "manifest exposes the additive async child-fetch RPC to the Lua wrapper"
      pattern: "DbeeConnectionGetColumnsAsync"
    - from: "dbee/handler/event_bus.go"
      to: "lua/dbee/ui/drawer/init.lua"
      via: "drawer consumes structure_loaded and structure_children_loaded by conn_id plus exact drawer-owned request_id, caller_token, and root_epoch fencing"
      pattern: "caller_token"
    - from: "lua/dbee/config.lua"
      to: "lua/dbee/ui/drawer/init.lua"
      via: "manual `R` still dispatches the drawer refresh action, but Phase 6 now constrains that action to D-63 nearest-ancestor targeted invalidation rather than global cache clear"
      pattern: "action = \"refresh\""
    - from: "lua/dbee/ui/drawer/init.lua"
      to: "lua/dbee/ui/drawer/convert.lua"
      via: "drawer branch state drives placeholder rows, deterministic sentinel IDs, raw-child caches, built-count tracking, and next-level cached materialization keyed by `(branch_id, kind)`"
      pattern: "structure_node_id"
    - from: "lua/dbee/ui/drawer/init.lua"
      to: "lua/dbee/ui/drawer/model.lua"
      via: "the sole renderer `build_tree_from_struct_cache(...)` rehydrates root and branch cache state for every full redraw, while child apply/error/load-more only invalidate the rendered snapshot"
      pattern: "build_tree_from_struct_cache"
---

<objective>
Deliver `STRUCT-01` without lying about non-blocking behavior and without reopening the Phase 7 lifecycle seams that caused repeated plan-gate churn.

Purpose: expanding connections, schemas, and table-like branches should feel materially more responsive on databases where full-tree root delivery is available because the drawer only materializes the next needed level, uses explicit caches, and fetches table/view children through an additive async flow rather than synchronous blocking RPCs. Phase 6 must state plainly that `connection_list_databases()` remains a separate residual sync seam on connection expansion, that the live claim here is drawer-local root-payload delivery rather than end-to-end expand paint, and that reconnect/source-reload ownership plus the remaining cross-module seams belong to Phase 7 (`DCFG-01`), not this plan.

Output: additive async child-fetch endpoint/event, additive drawer-owned `caller_token` and `root_epoch` fencing for root and branch payloads, a single `_struct_cache` drawer contract with explicit error storage, placeholder and `Load more...` UX, and headless/CI verification.
</objective>

<execution_context>
@/Users/naveenkanuri/Documents/nvim-dbee/.codex/skills/gsd-plan-phase/SKILL.md
@/Users/naveenkanuri/Documents/nvim-dbee/.codex/get-shit-done/workflows/plan-phase.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-structure-laziness-notes-picker/06-CONTEXT.md
@.planning/phases/06-structure-laziness-notes-picker/06-RESEARCH.md
@.planning/phases/04-drawer-navigation/04-CONTEXT.md
@.planning/phases/05-resilience-diagnostics/05-CONTEXT.md
</context>

<constraints>
- Honor D-30 through D-39, D-46 through D-47, D-49 through D-50, D-52, D-55 through D-56, and D-60 through D-63 in `06-CONTEXT.md` verbatim.
- D-48, D-51, D-53, D-54, D-57, D-58, and D-59 are deferred to Phase 7+ and MUST NOT be pulled back into Phase 6 implementation or validation by implication.
- Keep `connection_get_structure_async(conn_id, request_id?, root_epoch?, caller_token?)` backward compatible for existing consumers. Drawer-owned calls pass all four arguments; legacy callers such as LSP remain valid when they pass only `conn_id`.
- D-35 is binding: do not label table/view expansion "non-blocking" if it still calls synchronous `connection_get_columns()` on the expansion path.
- Cache keys must be collision-safe and rooted in the existing encoded drawer node/path identity. Do not key branch caches solely by display text.
- `Load more...` appends in the same branch. It must not reopen another picker, rebuild unrelated siblings, or lose cursor context.
- Root success/error applies MUST mutate only the affected connection subtree; they MUST NOT call global `refresh()` or re-enter `connection_list_databases()` for sibling connections.
- `DrawerUI:refresh()`, `show()`, `on_current_note_changed()`, and note-buffer `BufModifiedSet` refresh are the only Phase 6 presentation-only refresh callsites. Those paths MUST NOT clear `_struct_cache`; `on_current_connection_changed()` and `perform_action()` action callback refresh semantics are unchanged and remain Phase 7 (`DCFG-01`) territory per D-32, D-54, and D-57.
- All full redraws MUST flow through one canonical renderer, `build_tree_from_struct_cache(_struct_cache, ...)`. No other render path may read raw root cache directly.
- Manual `R` follows D-63 exactly: resolve the nearest ancestor connection from the current cursor node; warn and no-op if there is no connection ancestor.
- DRAW-01 zero-RPC typing remains locked. Filter typing must never call `connection_get_structure_async()`, `connection_get_columns()`, or the new async child fetch.
- While a filter session is active, branch-local async mutations update `_struct_cache` silently and do not patch the filtered tree. On filter close, restore the pre-filter rendered snapshot exactly per Phase 4 D-31; deferred branch mutations become visible only on the next regular refresh trigger or branch re-expansion after filter close, and filter close itself MUST NOT replay child RPCs.
- `lua/dbee/api/__register.lua` is generated, not hand-edited. Regenerate it with `cd dbee && go run . -manifest ../lua/dbee/api/__register.lua`, then verify `git diff --exit-code ../lua/dbee/api/__register.lua` stays clean.
- Phase 6 does not introduce or consume `connection_invalidated`, `_source_reload_silent`, `source_reload_failed`, or any public source-lifecycle invalidation/failure contract. Those are Phase 7 work.
- Phase 6 does not redefine `on_current_connection_changed()` or `perform_action()` action callback refresh semantics. Those callsites keep current behavior and Phase 7 (`DCFG-01`) owns their refresh contract per D-32, D-54, and D-57.
- Phase 6 does not invoke `rewrite_connection_identity()` from public reconnect flows, does not subscribe to `connection_rewritten`, and does not validate warm reconnect continuity. That limitation is deferred by D-62.
- No Phase 6 behavior depends on queued startup invalidation replay. D-52 remains a Phase 7 guardrail, not a new Phase 6 listener contract.
- Use the D-46 single-bump rule literally: bump `root_epoch` only during pre-request clear for a same-connection drawer-owned reload path, pass that value on the request, and never increment it again in the accept/apply path for that request.
- Headless replay/rebuild assertions MUST use the real `DrawerUI` refresh/restore path and real encoded `branch_id` values from `convert.structure_node_id(...)`; do not replace those surfaces with raw tree/materialization stubs in the cases that are specifically testing replay and rebuild semantics.
- Large-database root-warmup claims must follow D-55. If authoritative full-tree `structure_loaded` delivery does not complete within the live validation bound for an adapter, record the run as a caveated legacy-root path rather than as proof of improved end-to-end expand paint.
</constraints>

<interfaces>
Drawer-owned root request contract for Phase 6:
```lua
-- legacy callers remain valid:
handler:connection_get_structure_async(conn_id)

-- drawer-owned calls use the additive fence explicitly:
handler:connection_get_structure_async(conn_id, request_id, root_epoch, "drawer")

-- additive payload fields
{
  conn_id = "conn-id",
  request_id = 7,
  root_epoch = 4,      -- optional on legacy callers; used by drawer-owned loads
  caller_token = "drawer",
  structures = { ... },
  error = nil,
}

-- DrawerUI root apply gate:
payload.caller_token == "drawer"
  and payload.request_id == self._struct_cache.root_gen[conn_id]
  and payload.root_epoch == self._struct_cache.root_epoch[conn_id]
  and (self._struct_cache.root_applied[conn_id] or 0) < payload.request_id
```

Additive async child-fetch contract for Phase 6:
```lua
handler:connection_get_columns_async(conn_id, request_id, branch_id, root_epoch, {
  table = "employees",
  schema = "HR",
  materialization = "view", -- MUST be `struct.type` to preserve table vs view column fetch parity
  kind = "columns",
})

-- event payload
{
  conn_id = "conn-id",
  request_id = 3,
  branch_id = "drawer\x1fencoded\x1fpath",
  root_epoch = 4,
  kind = "columns",
  columns = { ... },
  error = nil,
}
```

Drawer-local cache contract for Phase 6:
```lua
self._struct_cache = {
  root = {
    [conn_id] = { structures = structures, error = nil },
  },
  root_gen = { [conn_id] = request_id },
  root_applied = { [conn_id] = request_id },
  root_epoch = { [conn_id] = epoch },
  loaded_lazy_ids = { [node_id] = true },
  branches = {
    [conn_id] = {
      [branch_id .. "\x1fcolumns"] = {
        raw = columns,
        error = nil,
        built_count = 1000,
        render_limit = 1000,
        request_gen = request_id,
        applied_gen = request_id,
        loading = false,
      },
    },
  },
}
```

Canonical full-redraw contract for Phase 6:
```lua
drawer_model.build_tree_from_struct_cache(
  handler,
  editor,
  result,
  self._struct_cache,
  current_conn_id,
  refresh_cb,
  opts
) -> DrawerRenderNode[]

-- This is the sole renderer allowed to rehydrate:
-- - root structures / root error rows
-- - branch-local loaded child rows
-- - branch-local error rows
-- - per-branch built_count / render_limit
-- - deterministic Load more... sentinel state
```
</interfaces>

<tasks>
<task type="auto">
  <id>06-01-01</id>
  <title>Add the truthful async child-fetch surface and drawer-owned root fencing</title>
  <files>dbee/endpoints.go, dbee/handler/handler.go, dbee/handler/event_bus.go, lua/dbee/api/__register.lua, lua/dbee/handler/init.lua, lua/dbee/doc.lua</files>
  <read_first>
    - .planning/phases/06-structure-laziness-notes-picker/06-CONTEXT.md
    - .planning/phases/06-structure-laziness-notes-picker/06-RESEARCH.md
    - lua/dbee/handler/init.lua
    - dbee/handler/handler.go
    - dbee/handler/event_bus.go
    - dbee/endpoints.go
    - dbee/main.go
  </read_first>
  <action>
    Add the minimum async/event surface that Phase 6 actually owns.

    1. Introduce a new additive async endpoint and handler wrapper for current table/view child truth:
       - `DbeeConnectionGetColumnsAsync`
       - `Handler:ConnectionGetColumnsAsync(...)`
       - `Handler:connection_get_columns_async(...)`
       - regenerated manifest entry in `lua/dbee/api/__register.lua`

    2. Carry `request_id`, `branch_id`, `kind`, `root_epoch`, and `materialization` through the child round trip.
       - `branch_id` originates in Lua from the encoded drawer node/path identity and is treated as an opaque token by Go
       - for table/view child families, `branch_id` MUST be exactly `convert.structure_node_id(parent_id, struct)`
       - `root_epoch` is compared in the drawer before any child payload applies
       - async table/view child requests MUST carry `materialization = struct.type` end-to-end so the new async path preserves the existing table vs view column semantics

    3. Extend the existing full-tree async surface with additive drawer-owned ownership and stale-load threading only.
       - `connection_get_structure_async()` accepts optional `request_id`, optional `root_epoch`, and optional `caller_token`
       - existing one-arg callers such as LSP remain valid and keep today's behavior when those extra args are omitted
       - drawer-owned calls pass `caller_token = "drawer"` and the current `root_epoch`
       - `structure_loaded` echoes additive `root_epoch` and `caller_token` when supplied
       - existing consumers remain valid because `conn_id`, `request_id`, `structures`, and `error` keep their meaning
       - `DrawerUI:on_structure_loaded()` will later use `caller_token == "drawer"` plus exact `request_id` and `root_epoch` as the root lifecycle fence

    4. Emit a new additive Lua event, `structure_children_loaded`, with:
       - `conn_id`
       - `request_id`
       - `branch_id`
       - `root_epoch`
       - `kind = "columns"`
       - `columns` or `error`

    5. Keep `structure_loaded` backward compatible. Do not reuse it for child payloads, and do not force LSP or current drawer root-load consumers to special-case subtree events.

    6. Make the manifest story explicit and generated.
       - regenerate `lua/dbee/api/__register.lua` with `cd dbee && go run . -manifest ../lua/dbee/api/__register.lua`
       - treat `dbee/main.go` as the canonical manifest entrypoint and `lua/dbee/api/__register.lua` as generated output, not hand-edited source
       - add a verify step that reruns the generator and asserts `git diff --exit-code ../lua/dbee/api/__register.lua`

    7. Document the new endpoint/events in `lua/dbee/doc.lua`, including:
       - `DbeeConnectionGetColumnsAsync` as the additive table/view child fetch, including required `materialization`
       - additive `root_epoch` and `caller_token` on drawer-owned full-tree `structure_loaded`
       - the exact drawer-owned root pending-request gate
       - the D-46 single-bump rule for drawer-owned `root_epoch`
  </action>
  <verify>
    <automated>
      grep -n "DbeeConnectionGetColumnsAsync\|ConnectionGetColumnsAsync\|materialization" dbee/endpoints.go dbee/handler/handler.go lua/dbee/api/__register.lua lua/dbee/handler/init.lua lua/dbee/ui/drawer/convert.lua
      grep -n "structure_children_loaded\|structure_loaded\|branch_id\|root_epoch\|caller_token\|kind = \\\"columns\\\"" dbee/handler/event_bus.go lua/dbee/doc.lua
      sh -c "cd dbee && go run . -manifest ../lua/dbee/api/__register.lua && git diff --exit-code ../lua/dbee/api/__register.lua"
    </automated>
  </verify>
  <acceptance_criteria>
    - `structure_loaded` remains the full-tree event used by current drawer/LSP paths, with additive `root_epoch` plus additive `caller_token` for drawer-owned loads only.
    - A new additive async child-fetch endpoint exists, preserves `materialization = struct.type` for table vs view requests, and does not change the synchronous `connection_get_columns()` contract for existing call sites that still need it.
    - `lua/dbee/api/__register.lua` is regenerated from `dbee/main.go` via `cd dbee && go run . -manifest ../lua/dbee/api/__register.lua`; drawer-side code does not depend on an unregistered RPC, and rerunning the generator leaves `git diff --exit-code ../lua/dbee/api/__register.lua` clean.
    - The new child event carries `conn_id`, `request_id`, `branch_id`, `root_epoch`, and `kind` so drawer code can reject stale and cross-branch payloads without scanning unrelated state.
    - The drawer accepts `structure_loaded` only when the payload matches `caller_token == "drawer"` plus the drawer-owned pending request token and the current `root_epoch`, so legacy or foreign root loads are ignored by drawer cache mutation without breaking their original consumers.
  </acceptance_criteria>
  <done>Phase 6 now has the truthful async/event plumbing it actually owns: table/view child results arrive through a new additive branch-scoped event keyed by request generation, root epoch, branch identity, and preserved `materialization`; full-tree root loads preserve the existing `structure_loaded` contract while adding drawer-owned `root_epoch` plus `caller_token` as stale-load and ownership fences; and the generated manifest stays source-of-truth clean after regeneration.</done>
</task>

<task type="auto">
  <id>06-01-02</id>
  <title>Refactor drawer materialization into branch-scoped lazy loading and bounded cache-backed mutation</title>
  <files>lua/dbee/config.lua, lua/dbee/ui/drawer/init.lua, lua/dbee/ui/drawer/convert.lua, lua/dbee/ui/drawer/model.lua</files>
  <read_first>
    - .planning/phases/06-structure-laziness-notes-picker/06-CONTEXT.md
    - .planning/phases/06-structure-laziness-notes-picker/06-RESEARCH.md
    - lua/dbee/config.lua
    - lua/dbee/ui/drawer/init.lua
    - lua/dbee/ui/drawer/convert.lua
    - lua/dbee/ui/drawer/model.lua
  </read_first>
  <action>
    Replace eager subtree hydration with authoritative next-level materialization plus branch-local async child loading, without reopening the deferred Phase 7 lifecycle seams.

    1. Introduce a single `_struct_cache` container in `DrawerUI`:
       - `root[conn_id] = { structures, error }`
       - `root_gen[conn_id]`
       - `root_applied[conn_id]`
       - `root_epoch[conn_id]`
       - `loaded_lazy_ids[node_id]`
       - `branches[conn_id][branch_id .. "\x1f" .. kind] = { raw, error, built_count, render_limit, request_gen, applied_gen, loading }`

    2. Make one canonical renderer responsible for every full drawer rebuild.
       - add `build_tree_from_struct_cache(_struct_cache, ...)` as the sole renderer used by `DrawerUI:refresh()`, snapshot restore, and any other full redraw path
       - the renderer MUST rehydrate both root cache state and branch cache state, including loaded children, branch error rows, `built_count`, `render_limit`, and deterministic `Load more...` sentinel state
       - forbid alternate render paths from reading raw root cache directly; current `model.lua`/`convert.lua` rebuild paths must funnel through the canonical renderer instead of bypassing branch state

    3. Lock `DrawerUI:refresh()` and the drawer-internal callsites Phase 6 owns to cache-preserving presentation refresh.
       - audit `on_current_note_changed()`, note-buffer `BufModifiedSet` refresh, and `show()`
       - each of those callers plus `DrawerUI:refresh()` itself MUST be annotated and implemented as presentation-only refresh
       - presentation refresh MUST NOT clear `_struct_cache`, branch request generations, or saved branch materialization state
       - only explicit authoritative invalidators may clear targeted state: manual `R` and `database_selected`
       - `on_current_connection_changed()` and `perform_action()` callback refresh behavior is explicitly NOT redefined in Phase 6; those callsites keep current behavior and Phase 7 (`DCFG-01`) owns their refresh contract per D-32, D-54, and D-57. If those existing callsites currently rebuild from authoritative cached structure data, that is acceptable for Phase 6; they MUST NOT be refactored into the new cache-preserving contract.

    4. Replace eager connection expansion with placeholder-first root warmup.
       - cold expand inserts a local loading row immediately
       - dispatch exactly one drawer-owned `connection_get_structure_async(conn_id, request_id, root_epoch, "drawer")`
       - repeated expand while the same root request is pending must not dispatch a duplicate root RPC

    5. Apply accepted drawer-owned root payloads only through the exact fence:
       - `caller_token == "drawer"`
       - `request_id == root_gen[conn_id]`
       - `root_epoch == root_epoch[conn_id]`
       - `root_applied[conn_id] < request_id`
       - accepted root success/error mutates only the targeted connection subtree and stores `{ structures, error }` in `_struct_cache.root[conn_id]`
       - the D-46 single-bump rule applies only to manual `R` and `database_selected`: bump `root_epoch` only during the pre-request clear and never in the accept/apply path for that request

    6. Replace synchronous table/view `lazy_children` fetch with additive async branch loading.
       - on first table/view expand, insert a branch-local loading row and dispatch `connection_get_columns_async` with `table`, `schema`, and `materialization = struct.type`
       - use `(branch_id, kind)` branch state for pending-request dedupe
       - `branch_id` MUST be exactly `convert.structure_node_id(parent_id, struct)`
       - while `loading == true`, rapid repeat expand on the same branch must dispatch zero duplicate child RPCs
       - branch error is stored in branch state, replaces only the branch-local loading row, and clears `cached_render_snapshot`

    7. Implement bounded materialization with a deterministic inline `Load more...` sentinel.
       - cache raw authoritative children in branch state
       - build at most the first 1000 `DrawerUINode`s on initial materialization
       - use `tree:set_nodes(...)` for targeted connection-subtree replacement
       - use `tree:remove_node(sentinel_id)` before append, `tree:add_node(node, parent_id)` for each new child chunk node, and `tree:add_node(new_sentinel, parent_id)` when more rows remain
       - repeated sentinel activation appends only the next chunk, never duplicates or strands the sentinel, and invalidates `cached_render_snapshot`

    8. Keep drawer-owned clears scoped and explicit.
       - manual `R` and `database_selected` clear only the targeted connection root+branch state
       - manual `R` resolves the owning connection by the D-63 nearest-ancestor rule:
         connection row -> reload that connection
         descendant structure row or `database_switch` row under a connection -> reload the nearest ancestor connection
         source/note/help/no-connection-ancestor row -> `utils.log("warn", "select a connection row to reload")` and no-op
       - prune flat `loaded_lazy_ids` using the exact encoded `ID_SEP` rule (`node_id == conn_id` or `node_id:sub(1, #conn_id + 1) == conn_id .. ID_SEP`)
       - clear saved table/view expansion IDs for that connection
       - bump `root_epoch[conn_id]` exactly once during that pre-request clear
       - run cleanup and targeted rerender before any fresh drawer-owned root request

    9. Preserve DRAW-01 filter contracts.
       - child apply/error/load-more invalidate only `cached_render_snapshot`
       - `cached_search_model` invalidates only on authoritative root replacement or future searchable branch kinds
       - while a filter session is active, branch-local async mutations update `_struct_cache` silently and MUST NOT patch the filtered tree in place
       - on filter close, the tree restores the pre-filter rendered snapshot per Phase 4 D-31; deferred branch mutations remain only in `_struct_cache`, become visible only on the next regular refresh trigger or branch re-expansion after filter close, and filter close MUST NOT trigger replay child RPCs
       - filter typing remains zero-RPC

    10. Leave deferred Phase 7 seams alone.
       - do not add a `connection_invalidated` listener
       - do not add `_source_reload_silent`, `source_reload_failed`, or any public source-lifecycle invalidation policy
       - do not invoke `rewrite_connection_identity()` or subscribe the drawer to `connection_rewritten`
       - do not modify `current_connection_changed` ownership in this plan
       - do not add drawer/LSP single-flight coordination
       - do not define source-action dispatcher modes, invalidation backpressure, or late-startup-event handling
  </action>
  <verify>
    <automated>
      grep -n "action = \"refresh\"\|manually refresh drawer" lua/dbee/config.lua
      grep -n "_struct_cache\|loaded_lazy_ids\|on_structure_loaded\|on_structure_children_loaded\|root_gen\|root_applied\|root_epoch\|request_gen\|applied_gen\|loading\|cached_search_model\|error\|build_tree_from_struct_cache\|select a connection row to reload" lua/dbee/ui/drawer/init.lua lua/dbee/ui/drawer/model.lua
      grep -n "__load_more__\|tree:add_node\|tree:remove_node\|tree:set_nodes\|Load more\|loading\\.\\.\\.\|connection_get_columns_async\|built_count\|render_limit\|structure_load_more\|structure_node_id" lua/dbee/ui/drawer/init.lua lua/dbee/ui/drawer/convert.lua
      ! grep -n "self\\.structure_cache\|self\\.loaded_lazy_ids\|self\\.structure_request_gen\|self\\.structure_applied_gen" lua/dbee/ui/drawer/init.lua
      grep -n "build_tree_from_struct_cache\|build_search_model" lua/dbee/ui/drawer/model.lua
    </automated>
  </verify>
  <acceptance_criteria>
    - Connection/schema expansion is next-level-only and table/view expansion is async rather than synchronous.
    - Stale child responses are rejected by branch-scoped generation plus `root_epoch`, and non-drawer root payloads cannot overwrite drawer root state.
    - Full drawer rebuilds are produced only by `build_tree_from_struct_cache(...)`, so branch-local loaded children, branch errors, `built_count`, and `Load more...` sentinel state survive unrelated redraws.
    - `DrawerUI:refresh()`, `show()`, `on_current_note_changed()`, and note-buffer `BufModifiedSet` refresh are presentation-only and preserve `_struct_cache`; unrelated redraws from those Phase 6-owned callsites do not replay child RPCs or silently drop branch state. `current_connection_changed` and source-action callback refresh behavior remains unchanged from current code, and Phase 7 owns their refresh contract.
    - `Load more...` uses the actual tree mutation primitives available in this repo, preserves branch context, patches only the targeted branch rather than globally refreshing the drawer, and bounds node construction to the next chunk only.
    - Root success/error apply uses targeted subtree mutation, never global `refresh()`, never re-enters sibling `connection_list_databases()` calls, and persists root/branch error state inside `_struct_cache` until a fresh authoritative success replaces it.
    - Manual `R` and `database_selected` clear only the correct connection-scoped state, manual `R` follows D-63 nearest-ancestor resolution exactly including `database_switch` rows, and both paths follow the single-bump `root_epoch` rule.
    - Filter typing stays zero-RPC after the structure-lazy refactor, `cached_search_model` rebuilds only on authoritative root/searchable-data replacement, and active filter views stay frozen while underlying branch-local mutations land in `_struct_cache`; filter close restores the Phase 4 D-31 pre-filter snapshot, and deferred branch mutations surface only on the next regular refresh or re-expand after filter close.
  </acceptance_criteria>
  <done>The drawer now treats structure as authoritative cached data plus branch-local async child state inside one `_struct_cache` container instead of eagerly materializing entire subtrees. `DrawerUI:refresh()`, `show()`, `on_current_note_changed()`, and note-buffer refresh are cache-preserving presentation refresh by default, every full redraw flows through one canonical `build_tree_from_struct_cache(...)` renderer that rehydrates both root and branch state, connection/schema expansion is cache-backed and next-level only, table/view expansion is truthfully non-blocking via additive async child fetch, drawer-owned root payloads are accepted only through the exact `caller_token == "drawer"` plus pending-request and `root_epoch` gate, root/branch errors remain explicit in `_struct_cache` across later rerenders, `Load more...` uses targeted branch mutation through the actual tree primitives available in-repo, manual `R` and `database_selected` clear only targeted same-connection state before refetch using D-63 nearest-ancestor targeting, and DRAW-01 filter typing remains zero-RPC on authoritative cached data while filter close restores the Phase 4 D-31 pre-filter snapshot rather than rebuilding from the latest cache.</done>
</task>

<task type="auto">
  <id>06-01-03</id>
  <title>Add headless coverage and CI for the narrowed drawer-local STRUCT-01 scope</title>
  <files>ci/headless/check_structure_lazy.lua, .github/workflows/test.yml</files>
  <read_first>
    - .planning/phases/06-structure-laziness-notes-picker/06-CONTEXT.md
    - .planning/phases/06-structure-laziness-notes-picker/06-VALIDATION.md
    - lua/dbee/ui/drawer/init.lua
    - lua/dbee/ui/drawer/convert.lua
    - lua/dbee/ui/drawer/model.lua
    - lua/dbee/handler/init.lua
  </read_first>
  <action>
    Create a dedicated headless structure-lazy suite and run it in CI, but only for the narrowed Phase 6 contract.

    1. Add `ci/headless/check_structure_lazy.lua` using the existing headless drawer pattern.

    2. Use the real replay/rebuild surfaces for the cases that care about replay and redraw semantics.
       - full-tree async `connection_get_structure_async`
       - additive async `connection_get_columns_async`
       - real `DrawerUI`, real `refresh()` path, real expansion restore, and real `convert.structure_node_id(...)` branch IDs for replay/rebuild assertions
       - targeted same-connection drawer invalidators (`R`, `database_selected`)
       - `connection_list_databases()` spies so root partial-mutation claims cannot false-pass by silently re-entering the deferred sync seam
       - search/filter entry points needed to prove zero-RPC typing remains intact
       - do not replace tree/materialization with raw stubs in the cases that are explicitly testing redraw, replay, or branch-state persistence

    3. Cover at least these cases:
       - `A1`: cold connection expand shows a loading row immediately and queues one drawer-owned root async request
       - `A2`: repeated expand while a root request is pending does not queue duplicate root requests
       - `A3`: cached schema/container expand materializes only the next level and issues no RPC
       - `A4`: cold table and cold view expand each show a loading row and queue async child fetch with `materialization = struct.type` instead of calling synchronous `connection_get_columns()`
       - `A5`: stale child response for the same branch/request family is ignored after a newer response applies
       - `A6`: manual `R` or `database_selected` bumps `root_epoch` only during the pre-request clear; a late pre-clear child response is dropped silently
       - `A7`: late or foreign `structure_loaded` is ignored unless it matches `caller_token = "drawer"` plus the drawer-owned pending request token and current `root_epoch`
       - `A8`: branch error replaces the placeholder row in that branch only, is stored in `_struct_cache.branches[conn_id][branch_key].error`, and clears `cached_render_snapshot`
       - `A9`: a root error is stored in `_struct_cache.root[conn_id].error` and survives five unrelated presentation refreshes without silently re-entering root or child RPCs
       - `A10`: five sequential presentation refreshes drawn only from `on_current_note_changed()`, note-buffer `BufModifiedSet` refresh, and explicit `show()` refresh preserve branch state and replay zero child RPCs; `current_connection_changed` and action callback refreshes are checked only for non-regression, not for the Phase 6 cache-preserving contract
       - `A11`: full redraw after branch async success rebuilds through `build_tree_from_struct_cache(...)` and still shows loaded columns plus the correct `Load more...` sentinel / built-count state
       - `A12`: large branch renders the first 1000 children, appends `Load more...`, and initial materialization constructs at most 1000 nodes
       - `A13`: selecting the sentinel three times mutates only the branch through the real tree primitive path, appends exactly the next chunk in-place without duplicates or stranded sentinel rows, and invalidates `cached_render_snapshot`
       - `A14`: manual `R` on a connection row reloads that connection, manual `R` on a descendant structure row or `database_switch` row reloads the nearest ancestor connection, and manual `R` on source/note/help rows warns and no-ops
       - `A15`: `database_selected` clears only the targeted connection caches, clears saved table/view expansion IDs, bumps `root_epoch` during pre-clear, rerenders before refetch, and does not re-enter sibling `connection_list_databases()` calls
       - `A16`: manual `R` after expanding multiple tables triggers zero replay child-fetch RPCs before the new root request settles
       - `A17`: while a filter session is active, child completion and `Load more...` update the underlying `_struct_cache` silently and keep the filtered view frozen; filter close restores the Phase 4 D-31 pre-filter snapshot exactly, triggers zero RPCs and no tree mutations beyond snapshot restore, and deferred branch mutations surface only after a later regular refresh trigger or branch re-expansion
       - `A18`: manifest contains `DbeeConnectionGetColumnsAsync`, `DrawerUI:new()` registers `structure_children_loaded`, rerunning `cd dbee && go run . -manifest ../lua/dbee/api/__register.lua` leaves `git diff --exit-code ../lua/dbee/api/__register.lua` clean, and the replay/rebuild cases use real encoded `branch_id` values from `convert.structure_node_id(...)`
       - `A19`: filter typing after lazy loads performs zero RPCs, rebuilds its authoritative search corpus only on authoritative root replacement or future searchable branch kinds, and refreshes only the rendered snapshot after Phase 6 child apply/error/load-more mutations

    4. Emit machine-readable markers:
      - `STRUCT01_ROOT_LAZY_OK=true`
      - `STRUCT01_CHILD_ASYNC_OK=true`
      - `STRUCT01_STALE_GUARD_OK=true`
      - `STRUCT01_ROOT_EPOCH_OK=true`
      - `STRUCT01_FULLTREE_EPOCH_OK=true`
      - `STRUCT01_ERROR_CACHE_OK=true`
      - `STRUCT01_PRESENTATION_REFRESH_OK=true`
      - `STRUCT01_REBUILD_FROM_CACHE_OK=true`
      - `STRUCT01_LOAD_MORE_OK=true`
      - `STRUCT01_LOAD_MORE_BUILD_BOUND_OK=true`
      - `STRUCT01_PARTIAL_MUTATION_OK=true`
      - `STRUCT01_ROOT_PARTIAL_MUTATION_OK=true`
      - `STRUCT01_CHILD_EVENT_WIRED_OK=true`
      - `STRUCT01_BRANCH_DEDUPE_OK=true`
      - `STRUCT01_MANUAL_RELOAD_OK=true`
      - `STRUCT01_MANUAL_R_TARGET_OK=true`
      - `STRUCT01_RELOAD_ZERO_REPLAY_OK=true`
      - `STRUCT01_RENDER_SNAPSHOT_INVALIDATION_OK=true`
      - `STRUCT01_FILTER_FREEZE_OK=true`
      - `STRUCT01_REAL_RENDER_PATH_OK=true`
      - `STRUCT01_FILTER_ZERO_RPC_OK=true`
      - `STRUCT01_FULLTREE_CONTRACT_OK=true`
      - `STRUCT01_ALL_PASS=true`

    5. Update `.github/workflows/test.yml` to run the new headless script.
  </action>
  <verify>
    <automated>
      sh -c "cd dbee && go run . -manifest ../lua/dbee/api/__register.lua && git diff --exit-code ../lua/dbee/api/__register.lua"
      sh -c 'out=$(nvim --headless -u NONE -i NONE -n --cmd "set rtp+=$(pwd)" -c "luafile ci/headless/check_structure_lazy.lua" 2>&1); printf "%s\n" "$out" | grep "^STRUCT01_ALL_PASS=true$" && printf "%s\n" "$out" | grep "^STRUCT01_ROOT_LAZY_OK=true$" && printf "%s\n" "$out" | grep "^STRUCT01_CHILD_ASYNC_OK=true$" && printf "%s\n" "$out" | grep "^STRUCT01_STALE_GUARD_OK=true$" && printf "%s\n" "$out" | grep "^STRUCT01_ROOT_EPOCH_OK=true$" && printf "%s\n" "$out" | grep "^STRUCT01_FULLTREE_EPOCH_OK=true$" && printf "%s\n" "$out" | grep "^STRUCT01_ERROR_CACHE_OK=true$" && printf "%s\n" "$out" | grep "^STRUCT01_PRESENTATION_REFRESH_OK=true$" && printf "%s\n" "$out" | grep "^STRUCT01_REBUILD_FROM_CACHE_OK=true$" && printf "%s\n" "$out" | grep "^STRUCT01_LOAD_MORE_OK=true$" && printf "%s\n" "$out" | grep "^STRUCT01_LOAD_MORE_BUILD_BOUND_OK=true$" && printf "%s\n" "$out" | grep "^STRUCT01_PARTIAL_MUTATION_OK=true$" && printf "%s\n" "$out" | grep "^STRUCT01_ROOT_PARTIAL_MUTATION_OK=true$" && printf "%s\n" "$out" | grep "^STRUCT01_CHILD_EVENT_WIRED_OK=true$" && printf "%s\n" "$out" | grep "^STRUCT01_BRANCH_DEDUPE_OK=true$" && printf "%s\n" "$out" | grep "^STRUCT01_MANUAL_RELOAD_OK=true$" && printf "%s\n" "$out" | grep "^STRUCT01_MANUAL_R_TARGET_OK=true$" && printf "%s\n" "$out" | grep "^STRUCT01_RELOAD_ZERO_REPLAY_OK=true$" && printf "%s\n" "$out" | grep "^STRUCT01_RENDER_SNAPSHOT_INVALIDATION_OK=true$" && printf "%s\n" "$out" | grep "^STRUCT01_FILTER_FREEZE_OK=true$" && printf "%s\n" "$out" | grep "^STRUCT01_REAL_RENDER_PATH_OK=true$" && printf "%s\n" "$out" | grep "^STRUCT01_FILTER_ZERO_RPC_OK=true$" && printf "%s\n" "$out" | grep "^STRUCT01_FULLTREE_CONTRACT_OK=true$" && ! printf "%s\n" "$out" | grep -E "FAIL=|Lua error|Traceback" && grep -q "check_structure_lazy.lua" .github/workflows/test.yml'
    </automated>
  </verify>
  <acceptance_criteria>
    - Headless tests prove connection/schema expansion is next-level-only and that both table and view expansion use the additive async child path with preserved `materialization`, rather than synchronous `connection_get_columns()`.
    - Headless tests prove stale child responses are rejected by branch-scoped generation guards, and non-drawer root payloads are rejected by the exact drawer-owned `caller_token == "drawer"` plus pending-request and `root_epoch` fence.
    - Headless tests prove root and branch errors are stored explicitly inside `_struct_cache` and survive unrelated rerender until a fresh authoritative success replaces them.
    - Headless tests prove the Phase 6-owned presentation refresh paths (`DrawerUI:refresh()`, `show()`, `on_current_note_changed()`, and note-buffer `BufModifiedSet`) preserve `_struct_cache`, and full redraws rebuild from `build_tree_from_struct_cache(...)` rather than dropping branch-local state. `current_connection_changed` and action callback refreshes are checked only for non-regression, not this contract.
    - Headless tests prove `Load more...` uses the actual in-repo tree mutation primitives, preserves branch context, patches only the targeted branch rather than globally refreshing the drawer, and bounds node construction to the next chunk only.
    - Headless tests prove root success/error apply uses targeted subtree mutation, never global `refresh()`, and never re-enters sibling `connection_list_databases()` calls.
    - Headless tests prove manual `R` and `database_selected` clear only the correct connection-scoped state, manual `R` follows D-63 nearest-ancestor resolution exactly including `database_switch` rows, follow the single-bump `root_epoch` rule, and preserve lazy child re-fetch semantics.
    - Headless tests prove active filter views stay frozen during branch-local async mutations, underlying `_struct_cache` still updates, filter close restores the Phase 4 D-31 pre-filter snapshot exactly, filter close itself triggers zero RPCs and no tree mutations beyond that snapshot restore, and deferred branch mutations surface only after a later refresh or re-expand.
    - Headless tests prove manifest/listener wiring exists for the additive child event and CI runs the new structure-lazy suite.
    - Headless tests prove replay/rebuild-sensitive assertions run through the real `DrawerUI` refresh/restore path and real encoded `branch_id` values, and that filter typing stays zero-RPC after the structure-lazy refactor.
  </acceptance_criteria>
  <done>`check_structure_lazy.lua` now proves only the final narrowed Phase 6 promise set: root loads are async and drawer-owned, cached branches materialize only the next level, every full redraw flows through `build_tree_from_struct_cache(...)`, the Phase 6-owned presentation refresh paths remain cache-preserving, table/view expansion uses additive async child fetch with preserved `materialization`, root payloads are accepted only through the exact drawer-owned `caller_token == "drawer"` plus pending-request and `root_epoch` gate, root and branch errors remain explicit in `_struct_cache`, manual `R` and `database_selected` clear targeted state before refetch using the D-46 single-bump rule plus D-63 nearest-ancestor targeting, generated-manifest drift is guarded by regenerate-plus-diff, `Load more...` patches only the targeted branch through the real tree primitives, active filter views stay frozen while underlying cache mutates, and filter close restores the Phase 4 D-31 pre-filter snapshot without replay RPCs or immediate branch-state reveal.</done>
</task>
</tasks>

<verification>
1. `grep -n "DbeeConnectionGetColumnsAsync\|ConnectionGetColumnsAsync\|materialization" dbee/endpoints.go dbee/handler/handler.go lua/dbee/api/__register.lua lua/dbee/handler/init.lua lua/dbee/ui/drawer/convert.lua` shows the additive async child-fetch path exists and preserves table/view `materialization`.
2. `grep -n "structure_loaded\|structure_children_loaded\|root_epoch\|caller_token\|branch_id\|kind\|materialization" dbee/handler/event_bus.go lua/dbee/doc.lua` shows the drawer-owned root and branch event contracts are documented and emitted.
3. `sh -c "cd dbee && go run . -manifest ../lua/dbee/api/__register.lua && git diff --exit-code ../lua/dbee/api/__register.lua"` proves manifest regeneration is the source of truth for `lua/dbee/api/__register.lua`.
4. `grep -n "_struct_cache\|loaded_lazy_ids\|on_structure_loaded\|on_structure_children_loaded\|root_gen\|root_applied\|root_epoch\|request_gen\|applied_gen\|loading\|cached_search_model\|error\|build_tree_from_struct_cache\|select a connection row to reload" lua/dbee/ui/drawer/init.lua lua/dbee/ui/drawer/model.lua` shows explicit drawer-local state ownership, the canonical renderer, root/branch event fencing, refresh semantics, and root/branch error storage.
5. `grep -n "__load_more__\|tree:add_node\|tree:remove_node\|tree:set_nodes\|Load more\|loading\.\.\.\|connection_get_columns_async\|built_count\|render_limit\|structure_node_id" lua/dbee/ui/drawer/init.lua lua/dbee/ui/drawer/convert.lua && grep -n "action = \"refresh\"" lua/dbee/config.lua` shows deterministic sentinel IDs, actual branch mutation, targeted reload mapping, bounded node construction, placeholder UX, and async child fetch usage.
6. `! grep -n "self\\.structure_cache\|self\\.loaded_lazy_ids\|self\\.structure_request_gen\|self\\.structure_applied_gen" lua/dbee/ui/drawer/init.lua` proves legacy top-level structure state is gone in favor of `_struct_cache`.
7. `grep -n "build_tree_from_struct_cache\|build_search_model" lua/dbee/ui/drawer/model.lua` confirms both canonical tree rebuild and authoritative search-model handling exist.
8. `check_structure_lazy.lua` passes only if output contains the narrowed marker set (`STRUCT01_ALL_PASS`, `STRUCT01_ROOT_LAZY_OK`, `STRUCT01_STALE_GUARD_OK`, `STRUCT01_ROOT_EPOCH_OK`, `STRUCT01_ERROR_CACHE_OK`, `STRUCT01_PRESENTATION_REFRESH_OK`, `STRUCT01_REBUILD_FROM_CACHE_OK`, `STRUCT01_LOAD_MORE_OK`, `STRUCT01_MANUAL_R_TARGET_OK`, `STRUCT01_FILTER_FREEZE_OK`, `STRUCT01_REAL_RENDER_PATH_OK`, `STRUCT01_FULLTREE_CONTRACT_OK`, and the remaining Phase 6 markers) and no `FAIL=` / `Lua error` / `Traceback`.
9. `.github/workflows/test.yml` includes `check_structure_lazy.lua`.
10. Manual live verification on a large schema confirms placeholder replacement, `Load more...` behavior, D-63 nearest-ancestor reload scoping (connection row, descendant structure row or `database_switch` row, and non-connection warn/no-op), and that the documented residual `connection_list_databases()` seam does not get misreported as fixed; if authoritative full-tree root delivery never lands within the 5s validation bound, record the adapter as the D-55 caveat rather than claiming STRUCT-01 root-payload-delivery success.
</verification>

<success_criteria>
- Expanding a connection shows a placeholder immediately and warms root structure asynchronously where authoritative full-tree root delivery lands within the D-55 bound, while the remaining `connection_list_databases()` seam is documented honestly as still synchronous and outside the end-to-end expand-paint claim.
- Cached schema/container branches expand from authoritative cache without recursive eager subtree materialization.
- Expanding a table/view branch never blocks on synchronous `connection_get_columns()` from the expansion path and preserves `materialization = struct.type` on the async child request.
- `structure_loaded` stays backward compatible for existing root/LSP consumers, including the current one-arg `connection_get_structure_async(conn_id)` callers, adds only additive drawer-owned `root_epoch` and `caller_token`, and is accepted by the drawer only when `caller_token == "drawer"` plus the exact pending request token also match.
- `DrawerUI:refresh()`, `show()`, `on_current_note_changed()`, and note-buffer `BufModifiedSet` refresh are the Phase 6-owned cache-preserving presentation refreshes, and every full redraw rebuilds from `build_tree_from_struct_cache(...)` so branch-local state survives unrelated redraws. `current_connection_changed` and source-action callback refresh behavior remains unchanged and deferred to Phase 7.
- `Load more...` mutates only the targeted branch through the actual tree primitives available in this repo, preserves cursor and expansion context, and builds only the next chunk's nodes.
- Root success/error mutates only the targeted connection subtree; sibling connections are not forced through global refresh or repeated `connection_list_databases()` calls.
- Explicit drawer-owned clears prune `loaded_lazy_ids` through the `ID_SEP` rule, clear saved expansion IDs in the correct scope, manual `R` resolves nearest ancestor connection per D-63 including `database_switch` rows, bump `root_epoch` only during the pre-request clear, and leave deeper child families lazy until they are expanded again after the fresh root request settles.
- Root and branch errors remain explicit in `_struct_cache` until a later authoritative success replaces them.
- Active filter views stay frozen during branch-local async mutations, underlying `_struct_cache` still updates, and filter close restores the Phase 4 D-31 pre-filter snapshot without replay RPCs; deferred branch mutations become visible only on the next regular refresh or re-expand after filter close.
- Large-database validation is caveated honestly per D-55: Phase 6 claims improved root-payload delivery only where authoritative full-tree root delivery succeeds within the live validation bound; legacy slow-path adapters are recorded, not misreported as passes, and the bound is not treated as proof that the remaining `connection_list_databases()` seam is solved.
- `/` filtering remains zero-RPC and searches only authoritative cached data.
- All structure-lazy headless tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/06-structure-laziness-notes-picker/06-01-SUMMARY.md`
</output>
